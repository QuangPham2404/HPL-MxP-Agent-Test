# Build Source

This directory records application-source expectations for builds. The
NVIDIA HPL-MxP implementation is distributed as a prebuilt package/container;
do not place source or artifacts here until an approved workflow requires it.
