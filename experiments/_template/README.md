# Experiment Template

Copy this template to a new kebab-case experiment directory only when an
experiment has been authorized. Record the purpose, binary, environment,
inputs, resources, validation, and any runtime error-patching history.
