# Progress — 2026-09-10 session 2 (internode-coms Phase B2 UCC ablation)

## Current step

Parent GPUDirect-RDMA debug track, Phase B2 **UCC ablation** (user-directed,
executed and analyzed this session): the two confirmed anomaly cases repeated
with `--mca coll ^ucc` on strictly pristine nodes. 6 jobs, 24 test executions,
all rc=0. **Split verdict: Case B is UCC-causal (anomaly disappears); Case A
is not UCC (anomaly persists).** Results in
`scripts/gaas-internode-coms-debug/DEBUG_PROGRESS.md` → Phase B2 → "UCC
ablation"; README resume point updated. Both clones synced.

## What was completed

- **Design (user decisions):** repeat the anomaly-resweep cases with the
  single delta `--mca coll ^ucc` on every mpirun; pristine nodes only
  (6 jobs — user chose the clean-node reading to remove contention noise);
  Case A (3x4 `osu_bcast -d cuda`) first, then Case B (3x1 `osu_allreduce -d
  cuda` through 1 MiB); everything else unchanged (pinned chunks, rank/GPU
  mapping, launcher, B2 diagnostics, H H controls, same-job GDR-on/GDR-off
  A/B, checkpoints + scheduler snapshots).
- **Script added:** `debug-scripts/phase1-step1/run_phase1_step1_collb2_
  uccabl.pbs` (resweep script + `--mca coll ^ucc`; verified diff = naming +
  the flag + metadata line only).
- **Runs (all rc=0, all on pristine g14+g16+g17, 3/3 pristine verified
  before every submission, zero foreign co-tenants at every checkpoint):**
  Case A jobs `61694/61695/61715.gaas`, Case B jobs `61717/61718/61719.gaas`.
  All evidence retrieved and byte-verified (only local-only sched-snapshot
  diffs, as designed).
- **UCC-exclusion proof:** ucc absent from `coll:base:comm_select`
  availability in every `.e` (vs "available: ucc, priority: 100" in all
  with-UCC runs); zero UCC team/score-map lines in every `.o` (vs 264 per
  resweep case-A job); fallback stack enabled: hcoll(90) > cuda(78) >
  tuned(30) > libnbc(10) > basic(10).
- **Verdicts (per the ablation design's interpretation rules):**
  - **Case B (3x1 allreduce @1 MiB): UCC/TL_UCP causal** — without UCC the
    anomaly disappears (+1.89…+1.97× healthy GDR; ctrl drops from
    1117–1310 µs to 74.5–75.0 µs, 15–17× improvement; sub-512 KiB gains
    survive at +1.89…+2.05×).
  - **Case A (3x4 bcast ≥8 MiB): NOT UCC** — anomaly persists and worsens
    (−2.46…−3.83×) under the UCC-free fallback stack; cause sits at/below
    the UCX transport-selection layer → next: UCX rail/rendezvous-threshold
    testing.
  - **UCC-exclusion is not a viable global mitigation:** degrades everything
    at 3x4 by 3–4.6× (cuda bcast ctrl @64 MiB 29.1k vs 6.3k µs; staged
    7.6k vs 2.1k; host H H 13.5k vs 4.4k). UCC provides large collective
    value on this stack.

## Evidence and blockers

- Evidence: `scripts/gaas-internode-coms-debug/outputs/phase1-step1/
  step1_collb2_uccabl_*` (6 attempts: `.o`/`.e`, ompiinfo/ucxdev/nvtopo,
  checkpoints, presched/postsched snapshots).
- Git: commits `c5e6373` (script), evidence + doc commits this session.
- No blockers.

## Exact next action

Await user direction on the recorded follow-ups (not executed): (1) Case A —
UCX rail/rendezvous-threshold testing at 3x4 (single-rail zero-copy GDR
selection is the standing suspect); (2) Case B — per-collective UCC tuning
or selective exclusion (not blanket `coll ^ucc`); (3) Track 2.2 re-scoped
in-container test (bcast-shaped residual, now known UCC-independent).
