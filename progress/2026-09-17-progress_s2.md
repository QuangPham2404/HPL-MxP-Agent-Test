# Progress — 2026-09-17 (NCCL 3x4 default-path smoke preparation)

## Current step

Prepare and run a separate functional `nccl-tests` smoke on 3 nodes × 4 GPUs.
This is distinct from the prior Phase 1 Step 2 transport-control matrix and
does not include performance comparison or tuning.

## Completed

- Read the required workflow files, application overview, latest progress
  report, nested debug instructions, and existing NCCL/Phase 1 launch scripts.
- Confirmed the existing Phase 1 Step 1 host-MPI + `rsh_pbsdsh.sh` pattern and
  the HPL-MxP model's hostfile/local-rank GPU mapping convention.
- Added `scripts/gaas-internode-coms-debug/debug-scripts/nccl-tests-smoke-3x4/`
  with a PBS script and README. The script runs one fixed 1 MiB all-reduce with
  12 ranks and default NCCL transport selection, logging backend/HCA evidence.
- Kept this smoke's script and evidence directory separate from
  `debug-scripts/phase1-step2/` and the build smoke.
- Committed and pushed the reviewed changes as `b34738d` to `origin/main`.
  The GAAS clone is not synchronized yet.

## Blocker and evidence

- `ssh -O check gaas` returned `No ControlPath specified for "-O" command`.
- Remote inspection, local-to-GAAS synchronization, `pbsnodes -aSj`, and PBS
  submission were not performed because the required persistent-connection
  preflight did not pass.
- The pre-existing untracked `hpl-mxp-runs-on-gaas/` directory was left
  untouched.

## Next action

Restore/configure the documented GAAS persistent SSH connection so
`ssh -O check gaas` succeeds. Then synchronize commit `b34738d` to GAAS with the
documented fast-forward procedure, probe `pbsnodes -aSj`, choose three clean
eligible nodes in `gpu_as` or `gpu_ded`, and submit only
`nccl_tests_3x4_smoke_v1` with PBS group `hpc_ebslee`.
