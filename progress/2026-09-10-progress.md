# Progress — 2026-09-10 (internode-coms Phase B2 anomaly resweep)

## Current step

Parent GPUDirect-RDMA debug track, Phase B2 **anomaly resweep** (user-directed
addition to Phase B2, executed and analyzed this session): two-arm repetition
series for the two residual GDR-on anomalies. 12 jobs, 48 test executions, all
rc=0. **Both anomalies confirmed as mechanism, not noise.** Results in
`scripts/gaas-internode-coms-debug/DEBUG_PROGRESS.md` → Phase B2 →
"Anomaly resweep" (with the new signed-ratio convention: +N = GDR faster,
−N = GDR slower); README resume point updated. Both clones synced.

## What was completed

- **Design (user decisions):** re-test the two Phase B2 residual cases —
  Case A (3x4 `osu_bcast -d cuda`, GDR-on −2.0/−2.5/−2.6× at 8/32/64 MiB) and
  Case B (3x1 `osu_allreduce -d cuda`, GDR-on −7.7× at 1 MiB) — ~3 reps each,
  in TWO arms: orig arm (g16+g17+g13, exactly the original anomaly conditions
  incl. g13's stable light co-tenant 59192) and pristine arm (strictly
  pristine g14+g16+g17, freed overnight; zero foreign co-tenants at every
  checkpoint). Separate jobs per rep (fresh allocation, per-rep GPU UUID
  evidence); same-job A/B (ctrl vs `UCX_IB_GPU_DIRECT_RDMA=n`) and full B2
  diagnostics retained; H H negative control per case.
- **Script added:** `debug-scripts/phase1-step1/run_phase1_step1_collb2_resweep.pbs`
  (parameterized `-v CASE=3x4bcast|3x1allred,ATTEMPT,REQ_HOSTS`, exp2-style
  submission with `-o/-e/-N` on the qsub line).
- **Runs (all 4 test executions per job rc=0):** orig arm jobs
  `61419-61424.gaas` (case A v1-v3 then case B v1-v3), pristine arm jobs
  `61425-61430.gaas` (same order). All evidence retrieved and byte-verified
  (the only md5 diffs were the 24 local-only presched/postsched snapshots,
  which never exist on GAAS by design).
- **Verdicts:** Case A = mechanism, co-tenant-independent — 6/6 reproduce
  (orig −2.49…−2.61× @64 MiB, ~±1% stable; pristine −2.95…−2.99×, slightly
  stronger; single-rail zero-copy `rc_mlx5` selection persists; UCC
  selection identical per rep). Case B = mechanism — 6/6 reproduce @1 MiB
  (orig −5.0…−10.7×, pristine −7.96…−9.34×; magnitude variable but direction
  never flips; ≤512 KiB behavior consistently healthy +2.13…+2.28×). H H
  controls ±1.00–1.08× everywhere.
- **Convention adopted (user instruction):** all newly presented ratios are
  signed (+ = GDR-on faster than GDR-off, − = slower); historical tables
  unchanged.

## Evidence and blockers

- Evidence: `scripts/gaas-internode-coms-debug/outputs/phase1-step1/
  step1_collb2_resweep_*` (12 attempts: `.o`/`.e`, ompiinfo/ucxdev/nvtopo,
  pre/prectrl/mid/post checkpoints, presched/postsched snapshots).
- Git: commits `162d350` (resweep script), evidence + doc commits this
  session; GAAS synced via documented duplicate-removal + `pull --ff-only`.
- No blockers.

## Exact next action

Await user direction on the recorded follow-ups (not executed): (1)
single-variable GDR-on + `--mca coll ^ucc` at 3x4/3x1 for both confirmed
residuals; (2) 3x4 bcast residual mechanism (single-rail zero-copy; rail/
memtype-cache/rndv-threshold knobs); (3) Track 2.2 re-scoped in-container
test for the bcast-shaped residual on clean nodes.
