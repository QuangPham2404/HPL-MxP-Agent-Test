# Progress — 2026-08-27 (N fine re-sweep)

## Current step

Step 2–6 complete for `matrix-placement-N-fine` (node-matched 1024-step sweep
below N=491520 under `--fill-device 1`). All ten attempts ran in one job,
retrieved, extracted, and analyzed as a follow-up section appended to
`planning/analysis/matrix-placement-N-resweep.md` (no new analysis file, per
user instruction). N dimension is now closed. Awaiting the user's next
direction.

## What was done this session

1. Staged `experiments/matrix-placement-N-fine/` (README, `run_N_fine.pbs`,
   `outputs/.gitkeep`). `run_N_fine.pbs` loops the full sequence in a single
   node-matched job: 491520 control → 8 fine N (490496…483328, 1024 steps) →
   491520 control. Committed `c29e99b`, pushed, remote pulled.
2. Submitted job 53334; all ten attempts ran on `hpc-gaas-g18` and PASSED.
3. Retrieved 10 `.o`/`.e` pairs via rsync; ran `extract_sweeps.py` (metrics.csv
   144 → 154 rows) and `generate_results.py`.
4. Appended a "Follow-up analysis (2026-08-27)" section to
   `planning/analysis/matrix-placement-N-resweep.md`; added a `PLANS.md`
   direction row and updated "Next direction".

## Key result

No fine peak below 491520. The eight fine points form a ~1% noise band
(2.3573e+06 … 2.3820e+06) with no trend. The single nominal excursion (484352 =
2.3820e+06, +0.12% over the start control) is inside the measured intra-node
drift: the same-node control fell 2.3792e+06 → 2.3706e+06 (−0.36%) over the
~24 min job. N=491520 + `--fill-device 1` stands; the N dimension is closed.

## PBS jobs this session

- 53334 — single node-matched job (hpc-gaas-g18) containing all ten attempts.

## Next action (needs user)

Propose the next orthogonal direction from `planning/PLANS.md`:
compute/precision (`--preset-gemm-kernel`, `--sloppy-type`, `--Anq-device`),
LU scheduling (`--use-separate-stream-for-gemm`, `--prioritize-trsm`,
`--prioritize-factorization`, `--u-panel-chunk-nbs`), or panel-broadcast
(`--use-mpi-panel-broadcast`).

## Notes / blockers

- Extraction `RESOURCES` column hardcodes `walltime=00:45:00` while this job
  used `02:00:00`; cosmetic metadata mismatch only (single node-matched job).
- Each `.e` contains only a harmless apptainer `unknown groupid` warning.
- GAAS still holds the 6 pre-existing untracked `N-resweep*_500000.{o,e}`
  artifacts (left untouched).
- Two untracked PBS job-level logs (`experiments/matrix-placement-N-fine/hpl_mxp_N_fine.{o,e}53334`)
  remain on GAAS; they are outside the designated `outputs/` directory and are
  left untouched (per-attempt evidence already lives in `outputs/`).