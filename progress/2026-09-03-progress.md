# Progress — 2026-09-03 (parameter dependency graph)

## Current step

The requested analysis-only reconstruction of HPL-MxP tuning-parameter
dependencies is complete. No new optimization plan, build, experiment,
scheduler job, remote action, source change, or results-table change was
performed.

## What was completed

- Reviewed the full numbered workflow pack, `APPLICATION.md`, the latest prior
  progress report, Git state, the tuning guide, `planning/PLANS.md`, all three
  consolidation reports, relevant detailed analyses, experiment READMEs,
  `results/metrics.csv`, and selected raw outputs.
- Created `planning/dependency-graph/README.md` with exactly the seven requested
  major sections.
- Created `planning/dependency-graph/edges.csv` as a machine-readable copy of
  the 36-edge dependency table.
- Distinguished observed, mechanistic, and uncertain interactions; retained
  negative results and their tested boundaries; and identified conclusions
  that must reopen for a 3-node × 4-H200 topology.
- Validated section count, edge-ID correspondence, local links, code fences,
  duplicate IDs, and trailing whitespace.

## Main findings

- `N`, `NB`, process grid/order, placement, residency, communication, and LU
  scheduling form a dependency chain; many old settings are closed only under
  the single-node 8×H200 control.
- The strongest observed cross-subsystem dependencies are the N/NB memory
  boundary, N-dependent grid reversal, N/residency LU-versus-solver trade-off,
  fill-buffer correctness cliff, communication/chunk structural interaction,
  and the factorization/TRSM 2×2 result.
- The biggest missing interactions are final-stack NB/grid revalidation,
  coordinated affinity/OpenMP placement, stream×priority scheduling,
  precision/kernel effects, partial `Anq-device` residency, and all multinode
  transport/mapping behavior.

## Evidence and blockers

- Deliverables: `planning/dependency-graph/README.md` and
  `planning/dependency-graph/edges.csv`.
- Original baseline retained throughout: `baseline-sweep_v1`,
  `1.4432e+06` GFLOP/s.
- There are no blockers. The worktree was clean at startup; the new analysis
  and progress files are currently untracked pending user review.

## Exact next action

Await user review of the dependency graph. Do not turn it into the future
optimization blueprint or stage new experiments without a separate request
and the required authorization.
