# Progress — 2026-08-20, session 12

## Current step

Corrected the baseline-comparison presentation in the `affinity` and
`fp64-matrix-mem-placement` analyses.

## Completed work

- Added the original-baseline percentage column directly to the existing
  affinity, OpenMP, default-fill, buffer-sweep, and register-step tables.
- Added a baseline table to the affinity analysis for readability.
- Kept the existing matrix baseline table and added the same comparison
  column to it.
- Removed the redundant standalone comparison tables created in the previous
  revision; no duplicate result tables remain.
- Kept the untested register-step percentage as `TBD`.

## Validation

- `git diff --check`: passed.
- Existing analysis tables now contain `Percentage vs original baseline`.
- No `Addendum` comparison section remains in either analysis file.

## Next action

Commit, push, and synchronize GAAS, then await further user direction.
