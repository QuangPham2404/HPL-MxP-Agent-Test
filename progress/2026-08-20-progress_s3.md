# Progress — 2026-08-20, session 3

## Current step

Step 5, Log results, is complete for the rerun outputs supplied in the sweep
experiment directories.

## Completed work

- Read the results schema and extraction/generation workflow.
- Validated the new stdout evidence: all extracted runs contain finite GFLOP/s
  markers and a `PASSED` verification marker.
- Reconciled `results/metrics.csv` from all retained sweep stdout files.
- Regenerated `results/RESULTS.md` from the CSV.
- Added 16 new attempt rows while preserving unique `(experiment_id, attempt)`
  keys and existing accounting metadata.
- Corrected extraction provenance for the two known retained PBS filename
  mismatches: N-sweep `*_402_re*.o` to `*_402k_re*.e`, and NB-sweep
  `n-resweep_399_{1,2,3}.o` to `n-resweep_399k_{1,2,3}.e`.

## Evidence and provenance

- Structured source: `results/metrics.csv` (35 rows).
- Generated report: `results/RESULTS.md`.
- Raw evidence: `experiments/{N-sweep,nb-sweep,np-sweep}/outputs/`.
- Extraction workflow: `results/scripts/extract_sweeps.py`.

## Validation

- Duplicate `(experiment_id, attempt)` keys: none.
- Missing referenced stdout/stderr files: none.
- Result statuses: 35 `completed`; verification: 35 `PASSED`.
- `git diff --check`: passed.

## Next action

Step 5 is complete. Await explicit analysis authorization before updating
planning or interpreting the rerun results.
