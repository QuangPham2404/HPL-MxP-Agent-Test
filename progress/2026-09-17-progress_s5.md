# Progress — 2026-09-17 s5 (Track 2 case resolution, GDR evidence chain, Phase 1 closure)

## Current step

Phase 1 of the internode-comms debug is CLOSED (user cross-checked and
confirmed): host GPUDirect RDMA is proven for both CUDA-aware MPI
(osu-cuda, Step 1) and NCCL (nccl-tests, Step 2). Next decision — Track 2.2
(in-container HPL-MxP GDR verification) or a deferred item — awaiting user
direction.

## Completed this session

- **Track 2 case `2026-09-17-A` investigated and RESOLVED** (user-directed):
  - Root cause (evidence-confirmed): the v1 ctrl 12-rank allreduce channel
    plan (g22+g20+g02) assigned g22 rank 1's inter-node links (channels
    04/0, 10/0) to `mlx5_bond_0` (Dev 8, RoCE, GDRDMA) while the peers used
    `mlx5_4` (IB) — link-type-incompatible connect → `ib_plugin.c:1312`
    reject → `transport/net.cc:555 → 3` (ncclInternalError) at first warmup
    collective. Only that arm used Dev 8; node-mix dependent.
  - Fix: `NCCL_IB_HCA` restricted to the eight IB HCAs (bond excluded) for
    both arms — wrapper `run_phase1_step2_gdr_coll_3x4_hca.pbs` + runner
    passthrough (`7cda615`); plugin honors the knob (verified in env table).
  - Validation: `step2_gdr_coll_3x4_v2` (job `67584.gaas`, same trio, PASS,
    zero bond channels). **Recovered cell: 3x4 allreduce 64 MiB algbw 43.92
    vs 18.86 GB/s (+2.33×; busbw 80.51 vs 34.57).**
  - Case record in root `MANUAL_INSPECTION_ERROR.md` → RESOLVED (includes
    upstream-report note).
- **GDR log-evidence chain documented** (user question): per-channel
  `/GDRDMA` graph tags (ctrl arms only; gdroff = 0 tags + `LOC` ENV proof +
  same IB backend) are the NCCL analog of UCX's zero-copy protocol lines;
  per-HCA "(DMABUF/peermem) enabled" lines are arm-independent capability
  probes, not usage proof. Recorded in `DEBUG_PROGRESS.md` (`eb97091`).
- **Phase 1 closure sections** added to `scripts/gaas-internode-coms-debug/
  README.md` and `DEBUG_PROGRESS.md` (this commit): combined conclusion —
  driver, GPU-memory registration (peermem/DMABUF), and the IB fabric
  deliver working GPUDirect RDMA through both stacks; 3x4/64 MiB GDR gains:
  sendrecv +54%, broadcast +151%, allreduce +133%.

## Final campaign table (algbw GB/s @64 MiB; `+N` = GDR-on N× faster)

| Rung | sendrecv | broadcast | allreduce |
|---|---|---|---|
| 2x1 | 24.63 vs 21.74 (+1.13×) | 48.28 vs 48.13 (+1.00×) | 28.43 vs 24.60 (+1.16×) |
| 3x1 | 24.54 vs 22.11 (+1.11×) | 38.69 vs 37.52 (+1.03×) | 23.39 vs 19.23 (+1.22×) |
| 3x4 | 40.78 vs 26.44 (+1.54×) | 70.88 vs 28.28 (+2.51×) | 43.92 vs 18.86 (+2.33×, bond-excluded v2) |

## Decisions

- User: investigate + solve case `2026-09-17-A` (done, validated by rerun).
- User: confirmed the GDR evidence chain and closed Phase 1 (both stacks).

## Blockers

- None open. Deferred (non-blocking): Case A UCX rendezvous-scheme/chunk
  experiment; Socket-floor control verification (`NCCL_IB_DISABLE=1`
  ineffective with IBext); upstream RoCE-bond rail-abort report;
  GDR-channel-fraction note (33–50%, deltas are lower bounds).

## Exact next action (resume)

Awaiting user direction: begin Track 2.2 (minimal in-container HPL-MxP GDR
A/B on clean 3x4 nodes — the strongest remaining lead for the slow
baseline), or address a deferred item.

## Evidence index

- All campaign evidence: `scripts/gaas-internode-coms-debug/outputs/phase1-step2/step2_gdr_*`
  (P2P v1/v2, coll v1/v2 — byte-verified) and
  `.../build-nccl-tests/outputs/nccl_tests_3x4_smoke_v*`.
- Records: debug `README.md` (attempt logs + Phase 1 closure),
  `DEBUG_PROGRESS.md`, root `MANUAL_INSPECTION_ERROR.md` (case RESOLVED).
- Key commits this session: `7cda615` (HCA fix), `7c0415e` (case resolution
  + recovered cell), `eb97091` (evidence chain), plus this closure commit.
