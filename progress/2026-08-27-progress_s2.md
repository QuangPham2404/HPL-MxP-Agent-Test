# Progress — 2026-08-27 (Nsight Systems profile of best config)

## Current step

Step 2 (prepare experiment) and Step 4 (execute) complete for a new
`experiments/nsight-systems/` profiling run. Nsight Systems captured a profile
of the current best HPL-MxP config (buffer + register step = 2048). Traces are
kept on GAAS only; the experiment is staged, run, and recorded. Awaiting the
user's next direction (kernel compute/scheduling analysis, per the plan).

## What was done this session

1. Created `experiments/nsight-systems/` (README, `run_nsight_systems.pbs`,
   `outputs/.gitkeep`) and a repo-root `.gitignore` covering `*.qdrep`,
   `*.qdrep-*`, `*.nsys-rep`, `*.qdstrm`, `*.sqlite`.
2. The run script wraps the best config in `nsys profile`
   (`--trace=cuda,nvtx,osrt,mpi --sample=cpu`), one profile per MPI rank via
   `-o .../hpl_mxp_rank_%q{OMPI_COMM_WORLD_RANK}`. GPU monitoring flags were
   dropped for a cleaner trace. Config: `--n 491520 --nb 3072 --nprow 2
   --npcol 4 --nporder row`, `--gpu-affinity 0:1:2:3:4:5:6:7`,
   `OMP_NUM_THREADS=8 OMP_PLACES=sockets OMP_PROC_BIND=TRUE`, `--fill-device 1
   --fill-device-buffer-size 2048 --cuda-host-register-step 2048 --skip-tests 1`.
3. Committed `5012345`, pushed, remote `git pull --ff-only`, submitted job 53452
   (node `hpc-gaas-g13`).

## Key result

- Job completed; 8 rank traces emitted as native `.nsys-rep` files (`~36–41 MB`
  each, 299 MB total) under `outputs/nsys-trace/`, plus `hpl_stdout.log`.
- HPL-MxP reported `PASSED` (residual `1.632064E-04`), GFLOPS `1.9668e+06`.
  The GFLOP/s is lower than the unprofiled ~2.37e+06 because of nsys overhead —
  expected for a profiling run and not a performance data point.

## Synchronization note

The remote `git pull` initially aborted because the generated `.o`/`.e`/
`hpl_stdout.log` files already existed untracked on GAAS (both clones hold the
same content). Resolved by backing them up and removing the untracked remote
copies, then fast-forwarding; the git-tracked versions (byte-identical) replaced
them. No evidence was lost.

## Git / PBS this session

- Commits: `5012345` (stage), `8788296` (record run evidence).
- PBS job: `53452.gaas` (hpc-gaas-g13, `hpl_mxp_nsys`).

## Notes / next action

- `.nsys-rep` traces remain on GAAS (`experiments/nsight-systems/outputs/nsys-trace/`),
  gitignored; analysis will be done over SSH (`nsys stats ...`).
- Pre-existing untracked GAAS artifacts (`N-resweep1/2/3_500000.{o,e}` and
  `matrix-placement-N-fine/hpl_mxp_N_fine.{o,e}53334`) were left untouched.
- Next: perform the nsys analysis of kernel compute/scheduling behavior and
  propose the next optimization direction, per `planning/PLANS.md`.