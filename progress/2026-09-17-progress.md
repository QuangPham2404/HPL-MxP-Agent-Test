# Progress — 2026-09-17 (NCCL v3 smoke and initial multinode sendrecv runs)

## Current step

Phase 1 — Step 2, host-only nccl-tests. The v3 build/smoke passed. The 2x1
and 3x1 sendrecv jobs ran; the 3x4 job is stopped pending resolution of an
unexpected NCCL transport in the socket-floor control.

## Completed work

- Fast-forwarded the GAAS clone from 1f81696 to 0f7217d, matching local
  main/origin/main. The two GAAS untracked v2 PBS outputs were preserved in
  build-nccl-tests/outputs/.pre-sync-v2/ before pulling their tracked copies.
- Submitted v3 as PBS job 67034.gaas. It ran on hpc-gaas-g06 with one H200.
  The build verified nine binaries; mpirun-launched osu_hello and all three
  all_reduce_perf arms returned 0. The arms reported the zero-error marker
  and the final build/smoke marker was BUILD_AND_SMOKE_OK.
- Confirmed that default NCCL diagnostics selected IBext_v11 and reported GDR
  enabled. This one-rank smoke did not exchange NCCL data across nodes.
- Prepared the shared sendrecv runner, three topology PBS wrappers, and
  topology README under scripts/gaas-internode-coms-debug/debug-scripts/phase1-step2/.
- Probed `pbsnodes -aSj` and selected the cleanest three-node `gpu_ded` set
  available: idle g21/g22 plus g03 (6/8 GPUs and 76/100 CPUs free). Ran 2x1 on
  g21/g22 and 3x1 on the same trio, one job at a time.
- Jobs 67037.gaas (2x1, 42 s) and 67038.gaas (3x1, 47 s) both exited 0; MPI
  launch/rank mapping and all sendrecv correctness markers passed.
- Both jobs' `NCCL_IB_DISABLE=1` socket-floor arms still loaded NCCL RDMA
  Plugin v11 and used `NET/IBext_v11` (the 3x1 arm also reported GDRDMA).
  The test runner's PASS marker checks correctness and exit codes but not the
  selected network, so neither topology is fully validated. The 3x4 job was
  not submitted after this transport mismatch.
- Updated the build attempt log and debug progress records.

## Evidence

- PBS job: 67034.gaas; queue gpu_as; project hpc_ebslee; node hpc-gaas-g06;
  PBS Exit_status=0; effective allocation one GPU, 12 CPUs, and 250 GB.
- Raw PBS files remain on GAAS:
  scripts/gaas-internode-coms-debug/build-nccl-tests/outputs/build_nccl_tests_host_v3.o
  and build_nccl_tests_host_v3.e.
- The draft matrix uses sendrecv_perf with three arms: default NCCL,
  NCCL_NET_GDR_LEVEL=LOC, and NCCL_IB_DISABLE=1.
- Multinode PBS output, per-arm NCCL logs, and pre/post scheduler snapshots
  remain on GAAS under scripts/gaas-internode-coms-debug/outputs/phase1-step2/.

## Validation

- bash -n passed for the shared runner and all three PBS wrappers.
- git diff --check passed for the complete tracked changes; new PBS and shell
  files also passed bash -n.
- GAAS bash -n passed for the transferred shared runner and three PBS wrappers.

## Current blocker and next action

The remaining blocker is that `NCCL_IB_DISABLE=1` did not force socket
transport with the loaded external RDMA plugin. Manually verify the supported
socket-selection control for this plugin before changing the arm or resuming
the matrix at 3x4. Do not automatically retry or modify the transport setting.
