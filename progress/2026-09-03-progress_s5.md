# Progress — 2026-09-03 (host-side GPUDirect-RDMA plan + git sync)

## Current step

Phase 4 investigation continues. Agreed with the user to verify GPUDirect RDMA
on the **host** first (no container). Discovered the available host tooling and
drafted the verification plan. Also cleaned up a GAAS-clone git pull conflict so
both clones are back in sync. No host GDR test executed yet.

## What was completed

- Discovered host-side GDR test tooling (read-only):
  - **Available/prebuilt:** HPC-X 2.25.1 `osu-micro-benchmarks-cuda`
    (`ompi/tests/osu-micro-benchmarks-cuda/{osu_bw,osu_latency,osu_bibw,...}`),
    `ucx_perftest` (`hpcx-2.25.1/ucx/bin`), host `mpirun`/`mpicc`/`ompi_info`
    (`hpcx-2.25.1/ompi/bin`), `nvcc` 13.1 (`nvhpc/26.3`).
  - **Absent:** `ib_write_bw`/perftest, `/usr/include/infiniband` (rdma-core
    headers). `libnccl.so.2` location on host unresolved (nccl-tests binary
    exists at `~/nccl-tests/build/all_reduce_perf` but links a missing
    `libnccl.so.2`).
- Drafted the host GDR verification plan (see below); awaiting user go-ahead.
- Resolved the GAAS-clone `git pull` conflict (see Git section).

## Host GDR verification plan (agreed direction, not yet executed)

1. **Step 0 sanity** — host-native 2-node `osu_hello`/`osu_allreduce` via
   `rsh_pbsdsh.sh` bridge to confirm real host MPI traffic flows (only spawn was
   validated before); plus `ompi_info --all | grep -i cuda` to confirm host
   OpenMPI CUDA-awareness.
2. **Step 1 primary** — `osu_bw D D` vs `H H` and `osu_latency D D`, 2 nodes × 1
   GPU (`CUDA_VISIBLE_DEVICES=0`). Device≈host ⇒ GDR working.
3. **Step 2 transport confirm** — `ucx_perftest -m host` vs `-m cuda`, optionally
   forcing `UCX_TLS=rc,dc` vs `rc,dc,cuda_copy`.
4. Launch: `select=2:ngpus=8:place=scatter`, `module load nvhpc/26.3`, host
   `mpirun` + `rsh_pbsdsh.sh` bridge (`plm_rsh_no_tree_spawn 1`,
   `plm_rsh_num_concurrent 1`, `routed direct`, `--bind-to none`).

## Git sync (resolved)

- GAAS clone was 2 commits behind (`f0928a1`, `59a265d`) and a plain `git pull`
  failed with "untracked working tree files would be overwritten by merge": the
  four probe-output files `scripts/outputs/comm_transport_probe_v{1,2}.{o,e}`,
  written by PBS jobs on GAAS, were untracked there but tracked upstream.
- Verified all four are **byte-identical** to the incoming tracked versions, so
  removing them loses nothing.
- Removed the four duplicates and ran `git pull --ff-only`. GAAS now at
  `59a265d`, tracking `origin/main`. The ~58 pre-existing untracked
  `experiments/*` artifacts remain untouched (per policy).

## Evidence and blockers

- Deliverables (this session): none new committed locally beyond the plan; the
  host GDR tooling findings and plan are recorded here. (Prior-session
  deliverables in `scripts/comm_transport_probe_report.md` and
  `scripts/outputs/comm_transport_probe_v{1,2}.{o,e}` remain tracked.)
- No blocker. Host GDR test not yet run.

## Exact next action

Await user confirmation on the host GDR plan (tools/topology/defaults), then
write and submit the host-only GDR probe (OSU-CUDA + UCX perftest, 2 nodes ×
1 GPU). Do not begin any tuning sweep without a separate request.