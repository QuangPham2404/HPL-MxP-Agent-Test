# Progress — 2026-08-26, session 5 (session close)

## Current step

Session close. The `matrix-placement-N-resweep` experiment (N sweep under
`--fill-device 1`) is fully staged, run, extracted, and analyzed. Both clones
are synchronized. Awaiting the user, who will revise the analysis plan and
propose the next optimization direction.

## What was done this session

1. **Staged** `experiments/matrix-placement-N-resweep/` (README, `run_N_resweep.pbs`,
   `outputs/.gitkeep`) — fixes `--fill-device 1` + default buffer/register + OMP
   8/sockets/TRUE and sweeps N. Committed `5a4acd1`, pushed, remote pulled.
2. **Ran** N ∈ {491520 (control), 460800, 430080, 399360, 368640}: jobs
   53190–53194, all PASSED.
3. **Retrieved** 5 `.o`/`.e` pairs; ran `extract_sweeps.py` (metrics.csv 139 →
   144 rows) and `generate_results.py`.
4. **Analyzed** `planning/analysis/matrix-placement-N-resweep.md`; updated
   `planning/PLANS.md`; committed `0521947`, pushed, remote pulled.

## Key result for this workload

The solver hypothesis is **mechanically confirmed but net-null**: the
iterative-solver time falls 13.07 → 4.09 s (share 39.2% → 29.1%) as N drops
491520 → 368640, but LU-efficiency loss at smaller N cancels the gain, so
GFLOP/s is flat within node noise. No peak below 491520. N=491520 +
`--fill-device 1` remains the recommended config (~2.37–2.39e+06, +66% over the
original 1.4432e+06 baseline).

## Final state

- Both clones (local PC and GAAS) at `0521947` on `main`.
- `results/metrics.csv`: 144 rows.
- Analysis: `planning/analysis/matrix-placement-N-resweep.md`; `planning/PLANS.md`
  carries the new direction row and next-direction narrative.
- Matrix-placement levers are now closed for this workload.

## Next action (needs user)

Revise the analysis plan and propose the next direction. Candidate orthogonal
levers already listed in `planning/PLANS.md`: compute/precision
(`--preset-gemm-kernel`, `--sloppy-type`, `--Anq-device`), LU scheduling
(`--use-separate-stream-for-gemm`, `--prioritize-trsm`,
`--prioritize-factorization`, `--u-panel-chunk-nbs`), and panel-broadcast
(`--use-mpi-panel-broadcast`).

## Notes / blockers

- Node noise (±3–7% across g16/g17/g18/g19) is the dominant error bar; the
  N=368640 vs N=491520 parity is not resolvable without repetition.
- GAAS still holds 6 pre-existing untracked `N-sweep`/`N-resweep*_500000.{o,e}`
  artifacts (left untouched).