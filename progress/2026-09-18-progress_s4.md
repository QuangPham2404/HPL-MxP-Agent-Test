# Progress — 2026-09-18 s8 (SingleNode-resweep: cmax 1x8 config scored — new record)

## Current step

`SingleNode-resweep` experiment complete through Step 5 (results logged).
The externally sourced "cmax" single-node configuration was scored as
`SingleNode-resweep_v1.1` — `PASSED`, `2.7694e+06` GFLOP/s, a new 1-node ×
8-GPU record. Step 6 (analysis) has NOT been performed: it awaits an explicit
`ANALYSE_RESULTS` authorization.

## Completed this session

- User-confirmed logistics: v1-only scope (no paired control), literal cmax
  replication (`--skip-tests 0`, `--monitor-gpu 0`), queues `gpu_as`/`gpu_ded`
  both allowed, group `hpc_ebslee`, experiment dir `experiments/SingleNode-resweep/`.
- Full startup reading (all workflow files, APPLICATION.md, latest progress,
  dependency graph, blueprint, single-node test log); Git state clean at
  `c85cbc7` (pre-existing untracked `hpl-mxp-runs-on-gaas/` preserved,
  untouched, reported).
- Created `experiments/SingleNode-resweep/` (README + parametrized
  `run_single_node_resweep.pbs` + `outputs/`), committed `9cf45b4`, pushed,
  remote pulled.
- Node survey: only pristine 8-GPU-free node in the allowed queues was
  `hpc-gaas-g22` (`gpu_ded`; gpu_as nodes maxed at 3/8 free GPUs;
  gpu_aisg pristine nodes off-limits). No active debug-workstream jobs.
- `SingleNode-resweep_v1` (`68348.gaas`, g22) FAILED at launch:
  `--preset-gemm-kernel: 90 not in {0,80}` — the v26.02 binary accepts only
  `{0,80}`; 90 is the *effective* SM90 preset from the default. Classified
  Track 1 (deterministic argument-contract defect); recorded in the
  experiment README with full evidence; patched by dropping the flag and all
  other unproven default-equal flags (defaults verified against the
  unmodified `baseline-sweep_v1` settings block); committed `2205d5d`;
  retried.
- `SingleNode-resweep_v1.1` (`68349.gaas`, g22, `gpu_ded`): **PASSED**,
  residual ratio `3.449666E-04` (finite, in tolerance),
  **`2.7694e+06` GFLOP/s** (per GPU `346,179.58`; LU `4.0464e+06`,
  per GPU `505,794.42`). Settings block verified identical to the cmax pod
  log. Phase timings: RNG 33.50 s, matgen 42.94 s, LU 7.46 s, solver 3.44 s;
  device 135.7/138.6 GB (full FP64 residency at N=356352), host 48.3 GB/rank.
  Evidence retrieved to local `outputs/` (both attempts + presubmit
  snapshots) and committed.
- Results logged (Step 5): extractor hardened (Track 1: append-only
  order-preserving merge, reviewed-metadata preservation, `queue=`/`resources=`
  echo parsing, `mpi_processes` derived from grid, `2Nodes-8GPUs` →
  `2x8-n-sweep` alias; verified idempotent, 190 existing rows byte-identical);
  appended 3 rows (v1 failed, v1.1 completed, plus the previously
  unextracted `3x4-smoketest/smoketest_100k_v1` with manually verified
  metadata); `RESULTS.md` regenerated (192 rows). Committed `f989271`, pushed.

## Key findings

1. **The cmax configuration is a new single-node record**: `2.7694e+06`
   GFLOP/s = **+91.87%** vs the original `baseline-sweep_v1` (`1.4432e+06`)
   and **+14.43%** vs the previous best `fp_0_1` (`2.4203e+06`). The +14.4%
   margin exceeds the 1-4% cross-node noise band (g22 vs g16), but no paired
   same-node control was run (user decision, v1-only scope).
2. The winning bundle changes five dimensions at once (N 491520→356352,
   grid 2x4→4x2 row, dgemv 0→15360, factorization 1→0, broadcast 50→100),
   so per-dimension attribution is unresolved; the dependency-graph edges
   listed in the experiment README (E07/E08/E09/E14/E20/E21/E25/E31) reopen
   if this direction is pursued.
3. v26.02 CLI contract: `--preset-gemm-kernel` accepts only `{0,80}`;
   effective preset 90 on H200 comes from the default. "Literal replication"
   of an external settings block must map to effective defaults, not CLI
   values, when the release differs.

## Decisions

- User: v1-only scope; literal cmax replication (skip-tests 0, monitor-gpu
  0); gpu_as/gpu_ded both allowed; group `hpc_ebslee`; dir name
  `SingleNode-resweep`.
- Agent (Track 1, recorded in experiment README): v1 argument-contract fix
  (drop `--preset-gemm-kernel 90` + default-equal unproven flags), retry as
  v1.1; extractor hardening to keep the CSV append-only and idempotent.

## Blockers

- None. (Remote clone: the untracked runtime evidence copies under
  `experiments/SingleNode-resweep/outputs/` were byte-identical to the
  committed blobs and quarantined as
  `outputs/.quarantine_identical_20260918` before pull; awaiting
  user-approved deletion, same precedent as phase2-stage2.)

## Exact next action (resume)

Awaiting user direction. If analysis is wanted, issue `ANALYSE_RESULTS`
(analysis_id `single-node-resweep`, source `results/metrics.csv`, include
`SingleNode-resweep_v1.1` + the single-node history; tables must show % vs
`baseline-sweep_v1` and vs the in-family control `fp_0_1`, with the
cross-node caveat). If the direction is pursued, the natural bounded next
experiments (not authorized, for review only) are per-dimension ablations of
the cmax bundle on a fixed node (e.g. dgemv 15360→0, factorization 0→1,
broadcast 100→50, grid 4x2→2x4 at N=356352) and an N re-check around
356352 under the new scheduling settings (E07/E14).

## Evidence index

- Raw evidence: `experiments/SingleNode-resweep/outputs/`
  (`SingleNode-resweep_v1.{o,e}` failed attempt, `SingleNode-resweep_v1.1.{o,e}`
  scored run, `_presubmit_v1.log`, `_presubmit_v1.1.log`).
- Records: `experiments/SingleNode-resweep/README.md` (config, dependency
  reopening list, attempt log, Track 1 record, result);
  `results/metrics.csv` (+3 rows); `results/RESULTS.md` (regenerated);
  `results/README.md` (extractor notes).
- Jobs: `68348.gaas` (v1, failed launch), `68349.gaas` (v1.1, PASSED), both
  `hpc-gaas-g22`, queue `gpu_ded`, group `hpc_ebslee`.
- Commits: `9cf45b4`, `2205d5d`, `f989271` (+ this progress commit).
