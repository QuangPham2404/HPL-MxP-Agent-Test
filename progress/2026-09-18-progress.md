# Progress — 2026-09-18 (resource-alloc exp5 mechanism study; 3x8 + 2x8 sweep holds)

## Current step

Resource-alloc experiment 5 (host-contention mechanism study) executed to
completion: preflight + 6 scored runs (3 pristine/busy pairs), all PASSED,
with full telemetry evidence. First-pass analysis recorded. The 3x8 N-sweep
and the 2x8 basic parameter sweep remain ON HOLD per user decision
(2026-09-17/18), with hand-off plans recorded in their READMEs.

## Completed work

- Wrote and validated 6 exp-5 scripts under
  `scripts/gaas-internode-coms-debug/resource-alloc/debug-scripts/`
  (probe, capture-mech, sampler-mech, pair-run pbs, preflight pbs, and a
  post-hoc analysis helper); committed, pushed, GAAS-synced before runs.
- Preflight (job 67780, g07): 2 s cadence validated; perf/pcm/PSI
  unavailable on GAAS compute nodes (recorded, non-fatal).
- Executed the series one job at a time (67781/67783/67784/67789/67790/
  67792): pristine arm = g11+g14+g12 (2 pristine + 1 light), busy arm
  adapted per live availability (r1 gpu_ded g03+g20+g22; r2/r3 gpu_as
  g08+g15+g12). All PASSED; evidence retrieved, committed (a966c95),
  pushed, GAAS-synced and sha-verified.
- One pre-authorized qdel: job 67782 (pinned g16 now `Qlist=gpu_aisg`;
  never started, no evidence lost; presubmit record preserved).
- Analysis (reproducible via `debug-scripts/analyze_mech_telemetry.sh`):
  dose-response refined, GPU starvation confirmed at 2 s resolution
  (clocks pegged), CPU-quota/cgroup/capacity/fabric-volume/NUMA/clocks
  ruled out, idle-holder paradox replicated, multi-minute TM-spawn delay
  on heavy nodes discovered, mechanism verdict UNRESOLVED per gates.
- Diagnosed (3 read-only probe jobs) and patched (Track 1, commit bedd599)
  a sampler defect: vmstat grep pattern missing `)`, error hidden by
  `2>/dev/null`; the node-global vmstat layer (incl. numa_hint_faults) is
  empty in all six runs. No silent re-runs; patched-pair validation
  proposed, awaiting user approval.
- Updated `resource-alloc/README.md` (exp-5 results section), parent
  `DEBUG_PROGRESS.md`, and this progress file.

## Key numbers (GFLOPS/GPU, N=250k)

- pristine arms: 44,820 / 45,885 / 45,585 (LU 15.0-15.3 s)
- busy r1 (mixed): 41,541 (LU 15.31)
- busy r2/r3 (2 heavy): 2,859 / 2,808 (LU 260.7 / 272.0)
- references: fully-pristine exp-3 trio ~71k (LU ~6.8); original
  contaminated baseline 3,341 (job 57232).

## Evidence

- `resource-alloc/outputs/mech_preflight_v1*` (4 files)
- `resource-alloc/outputs/mech250k_{pristine,busy}_r{1,2,3}*` (73 files:
  .o/.e, per-node pre/post/load logs, presubmit records incl. _a1)
- Probe jobs 67814/67815/67818 (vmstat-defect diagnosis) wrote only to
  GAAS /tmp (read-only diagnostics; conclusions recorded in the README).
- Commits: f9d9910 (scripts), a966c95 (evidence), bedd599 (sampler fix +
  analysis helper), 6e05ebe (DEBUG_PROGRESS), plus this file.

## Current blockers / open decisions

- Exp-5 surviving mechanism candidates (DDR bandwidth, PCIe/LLC, sub-2 s
  bursts, kernel auto-NUMA-balancing) need either the patched-sampler
  validation pair or a synthetic co-tenant experiment — user decision.
- 3x8 N-sweep: on hold (full-node availability), script ready on GAAS.
- 2x8 basic parameter sweep (NB bracket + grid/order): on hold, plan in
  `experiments/2Nodes-8GPUs/README.md`.
- Parent-track Phase 2 (in-container HPL-MxP GDR verification) is planned
  and awaiting user go (see DEBUG_PROGRESS.md resume point) — orthogonal
  to this track but expected to shrink every co-tenant effect measured
  here once the staged path is replaced by GPUDirect.

## Exact next action

Awaiting user direction: (a) exp-5 patched-sampler validation pair,
(b) synthetic co-tenant dose-response, (c) resume 3x8 N-sweep, (d) resume
2x8 basic parameter sweep, or (e) parent-track Phase 2.
