# Progress — 2026-08-31 (separate-stream-for-gemm: full workflow)

## Current step

Step 5 (results logging) and Step 6 (analysis) complete for the
`separate-stream-for-gemm` experiment. Awaiting user review.

## What was done this session

1. Staged `experiments/separate-stream-for-gemm/` (README + `run_separate_stream.pbs`
   + `outputs/`), toggling `--use-separate-stream-for-gemm` (0/1) at the current
   best config (factorization 1, N=491520, NB=3072, 2x4 row, fill-device 1,
   buffer/register 2048, default chunk/broadcast 8/50).
2. Committed/pushed (`a2f1ee2`), `git pull --ff-only` on GAAS, submitted
   PBS job `55620.gaas`, bounded-polled to completion (node `hpc-gaas-g16`).
3. Verified 2/2 `PASSED`, retrieved raw outputs to local.
4. Extended `results/metrics.csv` + `extract_sweeps.py` with
   `use_separate_stream_for_gemm`; updated `results/README.md` schema.
   Ran extraction (178 rows) and regenerated `results/RESULTS.md`.
5. Wrote `planning/analysis/separate-stream-for-gemm.md` and updated
   `planning/PLANS.md`.

## Results

| Attempt | ss | GFLOP/s | LU GFLOP/s | % vs original baseline | % vs ctrl (ss_1) |
|---|---:|---:|---:|---:|---:|
| ss_1 (control) | 1 | 2.4162e+06 | 4.1275e+06 | +67.42% | 0.00% |
| ss_0 | 0 | 2.3705e+06 | 3.9734e+06 | +64.25% | -1.89% |

Key finding: separate GEMM stream ON (default 1) is correct; disabling it loses
`-1.89%` end-to-end. LU-scheduling axis now closed; current best ≈ `2.416–2.420e+06`.

## Next action (for the continuing agent)

Report results to the user. Next orthogonal direction (pending user confirmation):
compute/precision levers (`--preset-gemm-kernel`, `--sloppy-type` FP4/FP8,
`--Anq-device`).

## Git / PBS this session

- Commit: `a2f1ee2` (stage); pending commit for results + analysis.
- PBS job `55620.gaas` (complete). Node `hpc-gaas-g16`.