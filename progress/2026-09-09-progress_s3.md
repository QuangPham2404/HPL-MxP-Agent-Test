# Progress — 2026-09-09 session 3 (resource-alloc experiment 4: N-sweep clean baseline)

## Current step

Resource-alloc investigation: designed, executed, and analyzed
**experiment 4 "N-sweep clean baseline"** (problem-size sweep on the
pristine trio, user-capped at N=510k). All 7 runs `PASSED`. The
resource-allocation track is now complete with its clean baseline curve.
Results in `scripts/gaas-internode-coms-debug/resource-alloc/README.md`.
Both clones synced at `5f456ab`.

## What was completed

- **Plan agreed with user:** sweep N = 450000..510000 (+10k, 7 jobs,
  capped to keep the count manageable — "diagnosing, not optimizing"),
  pristine trio g14+g16+g17, all other flags identical to experiments
  2-3, same instrumentation, carried-over operational policy. The earlier
  plan questions (memory limit, sweep driver) became moot with the 510k
  cap: projected host memory ~687 GB/node stays inside the mem=1000GB
  cgroup, and 7 jobs need no driver script.
- **Script added:** `debug-scripts/run_3x4_nsweep_clean.pbs` (exp3's
  script with N passed via `-v N=`); attempt labels `nsweep450k`..
  `nsweep510k`.
- **Runs (all `PASSED`, exit 0, pristine verified 3/3 before every
  submission, no co-tenant arrivals):**
  | N | GFLOPS total | GFLOPS/GPU | vs original baseline | LU GFLOPS/GPU | walltime |
  |---|---|---|---|---|---|
  | 450k | 1.8593e+06 | 154,942 | +4538% | 295,888 | 3:27 |
  | 460k | 1.9046e+06 | 158,714 | +4650% | 303,761 | 2:43 |
  | 470k | 1.9263e+06 | 160,527 | +4705% | 307,910 | 2:48 |
  | 480k | 1.9842e+06 | 165,350 | +4849% | 318,875 | 2:53 |
  | 490k | 2.0398e+06 | 169,981 | +4988% | 320,613 | 2:53 |
  | 500k | 2.1320e+06 | 177,667 | +5218% | 332,720 | 3:01 |
  | 510k | 2.1633e+06 | 180,272 | +5296% | 328,403 | 3:03 |
- **Key findings:** (1) clean curve rises 155 -> 180 TF/GPU headline,
  296 -> 328 TF/GPU LU-only. (2) **N=480k head-to-head: 49.5x the original
  contaminated baseline** (1.9842e+06 vs 4.0092e+04 total). (3) At ~490k
  the headline is ~62% of the single-node 8xH200 reference level while the
  LU-only rate matches/exceeds it — the residual gap is the comms ceiling
  (parent track), not resources. (4) Host-memory model (~10 + 2.6e-9 N^2
  GB/node) validated within ~1% on all 7 points; peak GPU memory
  70.3 -> 89.4 GiB/GPU; extrapolated walls: host cgroup ~N=617k, GPU HBM
  ~N=630k (the 510k cap sits ~20% below both). (5) 450k first-run warmup
  anomaly (RNG/matgen) noted and excluded from trend interpretation.
- **Git:** commits `89bf05c`, `5f456ab`; GAAS re-synced (77 evidence
  files verified byte-identical, remote duplicates removed, `pull
  --ff-only` clean).

## Evidence and blockers

- Evidence: `scripts/gaas-internode-coms-debug/resource-alloc/outputs/`
  (`nsweep450k..nsweep510k` with `.o/.e` + per-node `_pre_/_load_/_post_`
  logs).
- No blockers.

## Exact next action

Await user direction. The resource-allocation track is complete
(exp1 confound identified; exp2 co-tenant dose-response; exp3 placement
exonerated; exp4 clean baseline curve + 49.5x head-to-head). Open options
in the README: direct DDR/PCIe contention measurement; controlled
dose-response with synthetic co-tenants; admin guidance report;
re-validation after the parent track fixes in-container GPUDirect (the
remaining ~38% headline gap at ~490k belongs to that track; its resume
point is the Track 2.2 in-container GDR A/B test).
