# Progress — 2026-08-27 (MPI/NCCL panel-broadcast + U-panel chunk study)

## Current step

Step 2–7 complete for `experiments/mpi-nccl-coms-sweep/`: a two-phase
communication-behavior study swept `--use-mpi-panel-broadcast` (0/25/50/75/100)
and then `--u-panel-chunk-nbs` (4/8/16 at the two Phase-1 winners), all at the
fixed best config (N=491520, NB=3072, 2x4 row, fill-device-on, buffer+register
2048). Both levers are flat. Results extracted to `metrics.csv`, analysis
written to `planning/analysis/mpi-nccl-coms-sweep.md`, and `PLANS.md` updated.
Awaiting the user's next direction.

## What was done this session

1. Staged `experiments/mpi-nccl-coms-sweep/` (README, `run_broadcast_sweep.pbs`,
   `run_chunk_sweep.pbs`, `outputs/`). Added `use_mpi_panel_broadcast` and
   `u_panel_chunk_nbs` columns to `results/scripts/extract_sweeps.py` +
   `results/README.md`. Committed `749a1a4`, pushed, remote pulled.
2. Phase 1 (job 53508, node g13): broadcast 0/25/50/75/100 at chunk 8,
   bracketed by `bc_baseline_*` controls. All 7 PASSED.
3. Picked top-2 by GFLOP/s (`75` = 2.3570e+06, `50` = 2.3291e+06); committed
   `8fbccd2` with Phase-1 evidence + selected Phase-2 values.
4. Phase 2 (job 53546, node g13): chunk 4/8/16 at broadcast 75 and 50. All 8
   PASSED.
5. Extracted all 15 attempts to `metrics.csv` (170 rows total), regenerated
   `RESULTS.md`, wrote the analysis file, updated `PLANS.md`.

## Key result

Both parameters are flat within same-node drift (~2.6% across the `50/8`
controls). Broadcast mix moves only the LU phase by ≤2.3% (NCCL-only `0` =
2.3045e+06 worst, `75` = 2.3570e+06 soft peak; solver indifferent). U-panel
chunk 4/8/16 has no resolvable effect (all 2.3113–2.3496e+06 at both winners).
The MPI↔NCCL transport mix is not a first-order lever on this NVSwitch node.

## Git / PBS this session

- Commits: `749a1a4` (stage + extractor columns), `8fbccd2` (Phase 1 record +
  Phase 2 selection). Final commit for this handoff pending.
- PBS jobs: `53508` (broadcast sweep), `53546` (chunk sweep), both
  `hpc-gaas-g13`. Job `53545` was a stale-script submission (pre-pull) and was
  cancelled before it ran; `53546` is the re-submitted Phase 2.

## Notes / blockers

- Evidence-collision fix: Phase 2 originally reused the `baseline_a`/`baseline_b`
  labels, overwriting Phase 1's bracket controls. Fixed by renaming to
  `bc_baseline_*` (recovered from commit `8fbccd2`) / `chunk_baseline_*`, and
  removing two transient orphan rows from `metrics.csv`.
- Remote fast-forward twice aborted on untracked `outputs/*.{o,e}` created by
  the runs; both resolved by backing up and removing the untracked copies (no
  evidence lost) before `git pull --ff-only`.
- Pre-existing untracked remote artifacts (`N-sweep/N-resweep*_500000.{o,e}` and
  `matrix-placement-N-fine/hpl_mxp_N_fine.{o,e}53334`) were left untouched.
- `extract_sweeps.py` `RESOURCES` column still hardcodes `walltime=00:45:00`
  (cosmetic; both study jobs used `02:00:00`).

## Next action (needs user)

Pick the next orthogonal direction. This study closes panel-broadcast and
U-panel chunk as non-first-order for the current single-node workload; the
analysis recommends dependency priorities (`--prioritize-trsm`,
`--prioritize-factorization`) next, or a matched/nsys re-run of broadcast 75
vs 50 if a sub-noise transport signal is still wanted.