# Progress — 2026-09-16 (OSU-CUDA closure and NCCL pivot)

## Current step

Phase 1 — Step 1 (`osu-cuda`) is closed for the current host-side
CUDA-aware-MPI investigation. Phase 1 — Step 2 is the next branch, with
host-only `nccl-tests` scope. No jobs were submitted in this session.

## Completed work

- Updated `scripts/gaas-internode-coms-debug/DEBUG_PROGRESS.md` with the
  Step 1 closure and Step 2 pivot.
- Updated the debug `README.md` to mark Case A as deferred rather than lost.
- Preserved the known host NCCL prerequisite: the existing host
  `all_reduce_perf` needs `libnccl.so.2` before measurement.

## Decisions

- Pause further OSU-CUDA collective experiments for now.
- Keep Case A's UCX scheme/chunk investigation as a later follow-up.
- Test NCCL on the host first; do not mix container results into this branch.

## Blocker and exact next action

- **Blocker:** host `libnccl.so.2` is not currently available to the existing
  host `nccl-tests` binary.
- **Next action:** resolve that approved host-library dependency, then prepare
  a small host NCCL `sendrecv` test and record backend, HCA/rail, GDR, and A/B
  evidence before expanding coverage.

## Validation and Git

- Local documentation changes require review, `git diff --check`, commit, and
  push before the GAAS clone is synchronized.
- The pre-existing untracked `hpl-mxp-runs-on-gaas/` tree is preserved and is
  unrelated to this hand-off.
