# Progress — 2026-08-26, session 3 (matrix-placement-control re-run)

## Current step

Step 2–6 complete for the `matrix-placement-control` re-run at N=491520. All
18 new attempts ran, were retrieved, extracted into `results/metrics.csv`, and
analyzed in `planning/analysis/matrix-placement-491k.md`. Awaiting user
confirmation on the suggested next step (node-matched repetition to pin the
<1% buffer/register-step ties).

## What was done this session

1. Drafted `experiments/matrix-placement-control/run_matrix_placement.pbs`
   (parametrized by `ATTEMPT`, `N`, `FILL`, `BUF`, `REG`; fixed workload
   N=491520, NB=3072, 2x4 row, no affinity, OMP 8/sockets/TRUE) and appended a
   "Re-run at N=491520" section to the experiment README (preserving the old
   N=399360 history). Committed `5fae265` + `cca9575`, pushed, remote pulled.
2. **Task 1** (`fill_off` vs `fill_on`): jobs 53159–53160.
3. **Task 2** buffer sweep (`256,512,1024,2048,4096,6144,8192`, plus `fill_on`
   as the 3048 point): jobs 53163–53169.
4. **Task 3** register-step sweep (`512,1024,1536,2560,3072,3584,4096,4608,5120`
   at buffer 1024): jobs 53171–53179.
5. Retrieved all 18 `.o`/`.e` pairs via rsync; ran `extract_sweeps.py`
   (metrics.csv 121 → 139 rows) and `generate_results.py`.

## Key results

- `--fill-device 1` is a win: `491k_fill_on` = 2.3745e+06 (+2.33% over the
  2.3204e+06 fill-off shipping config), consistent with the prior N=399360
  result.
- Buffer optimum 1024–2048 (~2.39e+06, +0.7% over 3048); hard VRAM cliff at
  ≤512 → residual ~1.0e0 and **FAILED** verification.
- Register step is sub-noise (all within ±1.5% of 2048).
- Best valid: buffer 1024 + register 1536 = 2.3974e+06 (+66.12% over the
  original 1.4432e+06 baseline; +3.32% over the pre-experiment shipping config).
- No OOM fallback needed (fill-device=1 completed at N=491520).

## Next action (needs user confirmation)

1. Node-matched repetition of the top buffer/register candidates to separate
   the <1% ordering from ±3–7% node noise.
2. Then move to the remaining orthogonal levers at N=491520:
   compute/precision (`--preset-gemm-kernel`, `--sloppy-type`, `--Anq-device`),
   LU scheduling (`--use-separate-stream-for-gemm`, `--prioritize-trsm`,
   `--prioritize-factorization`, `--u-panel-chunk-nbs`), and
   panel-broadcast (`--use-mpi-panel-broadcast`).

## Notes / blockers

- Node noise is the dominant error bar: g10 returned an anomalous fill-off
  (2.1512e+06 vs 2.3204e+06 recorded on g17 for the same config), inflating the
  raw fill-on vs fill-off gap to +10.4% (real value ~+2.3%).
- GAAS still holds 6 pre-existing untracked `N-sweep`/`N-resweep*_500000.{o,e}`
  artifacts (left untouched).