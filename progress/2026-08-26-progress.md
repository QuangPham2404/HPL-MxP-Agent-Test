# Progress — 2026-08-26, OMP sweep completion

## Current step

Step 6/7 complete. Resumed and finished the `omp-sweep` experiment (OpenMP
thread/placement controls at N = 491520, nb = 3072, 2x4 row), extracted results,
wrote analysis `omp-sweep.md`, and updated `PLANS.md`. Awaiting user confirmation
on the next direction.

## What was done this session

1. **Resumed phase 1** (`OMP_NUM_THREADS` sweep) from the documented leave point:
   submitted `omp_t16`, `omp_t18`, `omp_t20` (jobs 52877–52879), all PASSED.
   Stop rule (two consecutive declines) triggered at t18 (2.2671e+06) / t20
   (2.2624e+06). Phase-1 peak = **t8 = 2.2859e+06**; t16 = 2.2840e+06 is a
   near-tie (0.08%, within node noise).
2. **Ran phase 2** (`OMP_PLACES ∈ {cores,sockets}` × `OMP_PROC_BIND ∈
   {TRUE,FALSE,CLOSE,SPREAD}`) at 8 threads (jobs 52880–52887), all PASSED.
   Best = **`sockets` + `TRUE` = 2.3204e+06**; `sockets` family 2.2987–2.3204e+06;
   `cores` + TRUE/CLOSE/SPREAD catastrophic (~0.9e+06).
3. **Retrieved** all 18 `.o`/`.e` pairs via rsync into
   `experiments/omp-sweep/outputs/`.
4. **Extracted** results: `results/metrics.csv` grew 103 → 121 rows;
   `results/RESULTS.md` regenerated (121 rows).
5. **Analyzed**: wrote `planning/analysis/omp-sweep.md`; added the direction row
   to `planning/PLANS.md` and rewrote the "Next direction" section.

## Key result

`OMP_NUM_THREADS=8` (+3.6% over no-OMP) combined with `OMP_PLACES=sockets` +
`OMP_PROC_BIND=TRUE` reaches **2.3204e+06 GFLOP/s**, +60.79% over the original
1.4432e+06 baseline and +5.15% over the previous best single result
(`np-sweep` 2x4_row_491k = 2.2067e+06). This is the highest single result for
this workload family.

## PBS jobs this session

- Phase 1: 52877 (t16), 52878 (t18), 52879 (t20).
- Phase 2: 52880–52887 (cores/sockets × true/false/close/spread at t8).

## Next action (needs user confirmation)

1. Repeat the top `sockets` candidates (`TRUE`, `SPREAD`, `FALSE`) on comparable
   nodes to separate their ~1% ordering from node noise.
2. Then layer the matrix-placement lever (`--fill-device-buffer-size` /
   register step) onto the finalized OpenMP setting at N = 491520.

## Notes / blockers

- Earlier-session defect (PBS clobbering `OMP_NUM_THREADS=96`) confirmed fixed:
   the `OMP_NT` re-export path produced correct per-run thread counts
   (`omp_num_threads` echoed 1..20 in every `.o`).
- Node noise remains ~±1.5%; single-run per cell, so the 1–2% spread among the
   top `sockets` candidates is not yet resolvable.
- GAAS still holds 6 untracked `N-sweep`/`N-resweep*_500000.{o,e}` user artifacts
   (left untouched).
- Local `todo.md` has unrelated pre-existing edits (not part of this work).