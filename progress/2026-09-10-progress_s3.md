# Progress — 2026-09-10 session 3 (internode-coms: Case B closure + Case A follow-up probe; hand-off)

## Current step

Parent GPUDirect-RDMA debug track. This session: (1) formally **closed Case B**
(3x1 allreduce @1 MiB) for the host track; (2) ran the user-directed
**Case A follow-up probe** (identify which UCX flag enables 2 rails) — outcome:
**the rail hypothesis is refuted by existing proto evidence** (the path is
already 2-rail 50/50), and the next experiment (rendezvous-scheme/chunk test)
is designed and queued in `DEBUG_PROGRESS.md`. Session ends here per user
request; this file is the hand-off.

## What was completed

- **Case B closure (user-confirmed):** recorded in `DEBUG_PROGRESS.md`
  Phase B2 → "Case B closure" (UCC/TL_UCP-causal; with UCC excluded the
  GDR-on path is healthy +1.9× and beats host buffers; blanket `coll ^ucc`
  ruled out as mitigation). README resume point restructured: Case A is the
  sole open host-track item. Commit `db497bc`.
- **Case A follow-up probe (read-only, no jobs):**
  - Verified this build's knobs via `ucx_info -c` (UCX 1.20.0 / HPC-X 2.25.1):
    `UCX_MAX_RNDV_RAILS=2` (default), `UCX_MULTI_LANE_MAX_RATIO=4.000`
    (the lane filter, per user's UCX-source correction),
    `UCX_RNDV_PERF_DIFF=1.000` (rndv-vs-eager protocol choice, not rail
    admission), `UCX_MAX_RMA_RAILS=1`, `UCX_RNDV_SCHEME=auto`,
    `UCX_MIN_RNDV_CHUNK_SIZE=16K`. Note: on the login node `ucx_info` needs
    `LD_LIBRARY_PATH=$HPCX/ucx/lib` prepended.
  - **Decisive finding:** re-parsing the proto evidence under the
    cuda-to-cuda inter-node configs shows the GDR data path is **already
    2-rail 50/50** (resweep ctrl 6.3 ms and ablation ctrl 29 ms both select
    e.g. `130..inf | zero-copy read from remote | 50% mlx5_4 + 50% mlx5_5`).
    The earlier "single-rail zero-copy" attribution was a mis-parse of
    eager/RMA/other-memtype rows — correction recorded in DEBUG_PROGRESS;
    performance numbers and verdicts stand.
  - Implication: a "force 2 rails" test is a no-op by construction; per the
    user's decision tree, Case A proceeds directly to the
    rendezvous/protocol-threshold test.
- **Queued next experiment (designed, not executed):** rendezvous-scheme/chunk
  test on the normal UCC-enabled path — V0 base / V1 `UCX_RNDV_SCHEME=put` /
  V2 `UCX_MIN_RNDV_CHUNK_SIZE=256K` (or 1M) / C gdroff staged control; same
  resweep structure (pristine 3x4 pinned nodes, bcast cuda sweep + H H
  control, same-job GDR-on/off A/B, `UCX_PROTO_INFO=y`, in-job `ucx_info -c`
  per variant, `UCX_MAX_RNDV_RAILS=2` kept per user instruction). If neither
  variant moves the gap, Case A is recorded as inherent GDR-read-path
  behavior.

## Decisions (user, this session)

- Keep UCC enabled and `UCX_MAX_RNDV_RAILS=2` for the first Case A
  experiment.
- UCX_RNDV_PERF_DIFF semantics correction accepted (protocol choice); the
  lane filter is UCX_MULTI_LANE_MAX_RATIO.
- Case B closed for the host track (optional UCC-side tuning noted,
  non-host-track).

## Evidence and blockers

- Probe evidence: live `ucx_info -c` output (quoted in DEBUG_PROGRESS) +
  existing `outputs/phase1-step1/step1_collb2_resweep_3x4bcast_pris_v1.o`
  and `step1_collb2_uccabl_3x4bcast_pris_v1.o` proto tables. No new jobs.
- Node state at hand-off: no strictly pristine trio (g14 fully held by job
  61743; g13 has two co-tenants; only g16+g17 pristine) — probe
  `pbsnodes -aSj` before the next submission; pristine-first with documented
  minimal-occupancy fallback applies.
- No blockers.

## Exact next action (resume point)

1. Ask the user to decide: skip the literal 2-rail test (recommended — no-op
   by construction, evidence recorded) or run it once for the record.
2. Then execute the queued rendezvous-scheme/chunk test exactly as designed
   in `DEBUG_PROGRESS.md` Phase B2 → "Case A follow-up probe" (V0/V1/V2/C
   matrix, one job, pristine nodes per probe; new script derived from the
   resweep template with per-variant UCX env).
3. After results: if a variant removes most of the gap → cause + mitigation
   identified → 3-rep confirmation; if neither → record Case A as inherent
   GDR-read-path behavior and close the host-track certification question
   with the user.
