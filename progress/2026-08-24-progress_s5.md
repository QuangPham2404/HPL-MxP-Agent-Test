# Progress — 2026-08-24, session wrap-up

## Current step

Step 7. Full session covers four sweeps at the current best workload, closing the
N/NB/grid/affinity tuning loop. Session is complete and synced; awaiting user
direction on the next direction.

## Completed work this session

1. **NB sweep** (`experiments/nb-sweep`, analysis `nb-sweep.md`): fixed
   N = 490000, swept nb = 1024 → 8192 in +1024 steps. Peak **nb = 3072**
   (2.1726e+06, +50.54% over baseline); two straight degradations at 7168/8192.
2. **N-NB resweep** (`experiments/N-nb-resweep`, analysis `N-NB-resweep.md`):
   fixed nb = 3072, swept N over multiples of 3072 from 488448. No meaningful
   `N % NB == 0` gain; peak **N = 491520** (2.1974e+06); OOM wall at 506880/509952.
3. **Process grid/order sweep** (`experiments/np-sweep`, analysis `np-sweep.md`):
   fixed N = 491520, nb = 3072, swept 2x4/4x2 × row/column. **2x4 row** best
   (2.2067e+06, +52.90%, the highest single result this session).
4. **CPU/memory affinity re-sweep** (`experiments/affinity-sweep`, analysis
   `affinity-491k.md`): fixed N = 491520, nb = 3072, 2x4 row. `--mem-affinity`
   −1.45% (dropped); `--cpu-affinity` neutral-to-negative, with <8 cores/rank
   catastrophic (2c −87%, 4c −18%). **No affinity is best.**

## Current recommended baseline

- **N = 491520, NB = 3072, 2x4 row, `--gpu-affinity 0:1:2:3:4:5:6:7`, no
  `--cpu-affinity`, no `--mem-affinity`** — ~2.20e+06 GFLOP/s (+52–53% over the
  original 1.4432e+06 baseline).

## Decisions

- mem-affinity dropped (no benefit).
- cpu-affinity closed (available cores under host-thread load are adequate at
  defaults; binding only hurts or ties).
- alignment (`N % NB == 0`) dropped as a lever.
- optimal grid is N-dependent (2x4 row at N = 491520 vs 4x2 col at N = 399360).

## Results artifacts

- `results/metrics.csv`: 103 rows; `results/RESULTS.md` regenerated.
- Analysis files: `nb-sweep.md`, `N-NB-resweep.md`, `np-sweep.md`,
  `affinity-491k.md`; `planning/PLANS.md` updated throughout.

## PBS jobs

nb-sweep 52247–52254; N-nb-resweep 52263–52270; np-sweep 52288–52291;
affinity 52305–52311, 52315–52316.

## Blockers / notes

- GAAS still holds 6 untracked `N-sweep/N-resweep*_500000.{o,e}` files (user
  artifacts, left untouched).

## Next action

Await user confirmation. Remaining untested lever at N = 491520 is the
matrix-placement family (`--fill-device-buffer-size`, register step), which
previously reached ~2.20e+06 at smaller N.