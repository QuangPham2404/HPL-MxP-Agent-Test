# Progress — 2026-09-14 (multinode workflow setup review)

## Current step

Step 1/2 documentation and workflow setup for the NVIDIA HPL-MxP 3-node ×
4-GPU campaign. No remote commands, builds, scheduler submissions, output
retrieval, or optimization runs were performed.

## Completed work

- Read all numbered workflow files `00`–`08`, `APPLICATION.md`, root
  `AGENTS.md`, and the latest prior handoff
  `progress/2026-09-10-progress_s3.md`.
- Reviewed the HPL-MxP sweep blueprint, dependency graph and `edges.csv`,
  existing 3×4 baseline analysis, and validated GAAS multinode launch models.
- Added `workflow/08-Workflow-Multinode-Tuning.md` with the GAAS container-MPI
  launch gates, topology directory contract, multinode evidence requirements,
  authorization boundaries, and dependency checkpoints.
- Added README contracts for `experiments/3Nodes-4GPUs/` and
  `planning/analysis/3Nodes-4GPUs/`.
- Added `SWEEP_WORKFLOW_FOR_USER.md`, a concise quick-start guide followed by
  the complete Phase 1 user/agent interaction example.
- Updated `AGENTS.md`, `workflow/README.md`, the blueprint, and dependency graph
  to remove authorization/baseline ambiguity and repair broken blueprint links.

## Decisions

- `multi-node-test/` remains the reusable launch-validation/model area.
- New scored 3-node × 4-GPU run families use
  `experiments/3Nodes-4GPUs/<run-name>/`; matching analyses use
  `planning/analysis/3Nodes-4GPUs/`.
- `3x4-baseline_v1` remains the immutable new-system original baseline for
  this campaign. Historical single-node `baseline-sweep_v1` is context only,
  not the percentage denominator.
- The validated container `mpirun` + container `orted` + `pbsdsh` bridge,
  `place=scatter`, no `mpiprocs`, de-duplicated hostfile, and sequential
  submission are the default GAAS multinode launch contract.

## Validation and state

- `git diff --check` passed.
- Existing multinode shell/PBS model scripts passed `bash -n`.
- Worktree was already dirty only because of the preserved untracked
  `hpl-mxp-runs-on-gaas/` tree; it was not inspected for modification or
  changed.
- Changes are not committed or pushed because this session did not authorize
  Git publication.

## Exact next action

Review the documentation diff and authorize commit/push if accepted. For the
next execution session, resume with Phase 0 evidence/transport characterization
and create the first scored child run under
`experiments/3Nodes-4GPUs/`, while keeping the pending host-track rendezvous
probe from the 2026-09-10 handoff as a separate decision.
