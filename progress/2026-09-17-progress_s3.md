# Progress — 2026-09-17 s3 (NCCL GDR A/B: smoke relocation, 3x4 smoke, P2P ladder start)

## Current step

Phase 1 — Step 2 NCCL GDR A/B experiments (plan in
`scripts/gaas-internode-coms-debug/README.md` → "NCCL GPUDirect RDMA A/B
experiment plan"). P2P ladder: 2x1 rung complete; 3x1/3x4 paused on node
saturation by user decision. Collective ladder not started (awaits P2P
report + user go).

## Completed this session

- Relocated the 3x4 nccl-tests smoke into self-contained
  `build-nccl-tests/scripts/` per user logistics correction; old
  `debug-scripts/nccl-tests-smoke-3x4/` preserved as
  `debug-scripts/[IGNORE]nccl-tests-smoke-3x4/` (commit `8a1c5af`).
- Ran the 3x4 functional smoke to PASS (`nccl_tests_3x4_smoke_v3`, job
  `67419.gaas`, pinned gpu_ded g01+g22+g20) after two Track 1 fixes (v1:
  module load before mpirun preflight; v2: `NCCL_TESTS_DEVICE=0` under
  per-rank `CUDA_VISIBLE_DEVICES` — nccl-tests defaults `cudaDev=localRank`).
  **Answer: default NCCL selects `NET/IBext_v11` with GPUDirect RDMA
  enabled (channels `via NET/IBext_v11/N/GDRDMA`); Socket not selected.**
  Evidence committed (`d1b5f36`).
- Recorded the agreed NCCL GDR A/B plan (6 jobs: P2P
  `sendrecv_perf` 2x1→3x1→3x4, then collective `broadcast_perf` root 0 +
  `all_reduce_perf` 2x1→3x1→3x4; ctrl vs `NCCL_NET_GDR_LEVEL=LOC`; job
  granularity mode×topology per user choice) in the debug README (`c411dcb`).
- Implemented the parameterized shared runner (`TESTS`/`ARMS`/`RESULT_TAG`,
  defaults preserve the original 3-arm sendrecv behavior) + 3 P2P wrappers
  (`cab6ac6`).
- **P2P 2x1 rung: COMPLETE, valid GDR A/B, GDR wins at large messages.**
  - v1 (`67456.gaas`, g22 pristine + g20): PASS; ctrl 16 via-IBext channels
    with 8 GDRDMA vs gdroff 16/0; GDR-on algbw +11–17% at ≥1 MiB (64 MiB
    24.72 vs 22.06 GB/s), neutral ≤512 KiB. Fabric evidence lost to
    inherited Track 1 defect (`OUTDIR` unexported; jobs 67037/67038 lost
    theirs identically) — patched `2972fc9`.
  - v2 (`67488.gaas`): PASS, scored attempt; same transport evidence,
    numbers reproduce v1, fabric evidence captured (nvidia_peermem loaded,
    8 HCAs). Evidence committed `9ed2dd4`.
- Sync procedure fix learned: `git status --short` collapses fully-untracked
  directories — use `-uall` when resolving GAAS pull collisions with
  byte-verified untracked evidence files (also fetch before comparing
  against `origin/main`).

## Decisions

- User: 6 jobs (mode × topology), broadcast root 0, gpu_as/gpu_ded only,
  group hpc_ebslee, cleanest-node policy, record co-tenant entry and never
  qdel running jobs.
- User: on 3x1/3x4 node saturation — pause and resume later (queued-pinned
  submission declined).

## Blocker

- 3x1/3x4 P2P rungs: no third gpu_ded/gpu_as node with ≥4 GPUs + ≥48 CPUs +
  ≥1 TB all evening (~3.5 h polling). g22 (pristine) + g20 (7/8) stable and
  idle; g01 peaked 3/8 GPUs + 40/100 CPUs + 765 GB behind the kng122 series
  and the 66066 array treadmill; g02/g03 CPU/mem short with 48h–1440h jobs;
  gpu_as nodes all ≤3/8; pristine g16–g19 are `gpu_aisg` (off-scope).
  Not a hard blocker — resource availability only.

## Exact next action (resume)

1. `ssh gaas` (direct form per user direction), `pbsnodes -aSj`.
2. Submit `step2_gdr_p2p_3x1_v1` from
   `scripts/gaas-internode-coms-debug/debug-scripts/phase1-step2/` on the
   cleanest feasible gpu_ded trio anchored on g22+g20 (+ third node
   whichever frees first; g01 needs one more of its 5 long jobs to end),
   with presched snapshot and attempt-specific `-o/-e`.
3. Monitor (bounded), postsched, validate (`STEP2_GDR_P2P_RESULT=PASS`,
   `transport_summary` gate: ctrl gdrdma ≥1, gdroff =0, both via_ibext >0).
4. Then `step2_gdr_p2p_3x4_v1` the same way (re-probe; same trio if still
   feasible), retrieve + byte-verify evidence, record, commit/push/sync.
5. Report P2P results to user; collective ladder (`step2_gdr_coll_*`
   wrappers — NOT yet created; add after P2P validates per plan) awaits
   user go.

## Evidence index

- Smoke: `scripts/gaas-internode-coms-debug/build-nccl-tests/outputs/nccl_tests_3x4_smoke_v{1,2,3}*`
- P2P 2x1: `scripts/gaas-internode-coms-debug/outputs/phase1-step2/step2_gdr_p2p_2x1_v{1,2}*`
- Attempt records: `debug-scripts/phase1-step2/README.md` → "P2P attempt
  log"; `build-nccl-tests/README.md` → smoke attempt log; `DEBUG_PROGRESS.md`.
- Key commits: `8a1c5af` (relocation), `b0e1eb0`+`0862f15` (smoke fixes),
  `d1b5f36` (smoke PASS), `c411dcb` (plan), `cab6ac6` (runner+wrappers),
  `2972fc9` (OUTDIR fix), `9ed2dd4` (2x1 record).
