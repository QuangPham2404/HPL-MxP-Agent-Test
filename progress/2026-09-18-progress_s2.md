# Progress — 2026-09-18 s6 (resource-alloc exp6: single-node contention test)

## Current step

Experiment 6 (single-node node-contention test) **complete**: full 6-run
matrix + one validation rerun, all `PASSED` (the light_4r cell was fielded
on attempt a3 after user approval, with a moderate 3-co-tenant composition
recorded as a caveat). The ctrl_2r_v1 anomaly is **explained and
reproduced** (app-side NCCL-topology CPU pinning to a tiny set; verify runs
`snver200k_2r_*`). Results recorded in
`scripts/gaas-internode-coms-debug/resource-alloc/SINGLE_NODE_TEST.md`.
Awaiting user direction on the remaining suggested next steps (mitigation
test, parent-track post-GDR pair, retrospective audit).

## Completed this session

- Design finalized with user: N=200k, mem=500GB/chunk, single pass,
  2r/4r on pristine control (g11) + heaviest feasible (g03 idle-holder)
  + lighter occupied (g12); grids 2x1 (2r) / 2x2 (4r); full execution
  chain authorized.
- Files created/updated (commit `dcb2c12`, runner hardening `1af2085`):
  `resource-alloc/SINGLE_NODE_TEST.md` (dedicated planning+execution log,
  kept separate from exp5 records by design),
  `resource-alloc/debug-scripts/run_1n_contention.pbs` (single-node runner
  reusing exp5 capture/sampler instrumentation unmodified), Experiment-6
  pointer in `resource-alloc/README.md`, role lines in
  `resource-alloc/AGENTS.md` and root `AGENTS.md`.
- Runs (all `PASSED`, exit 0; jobs
  67828/67835/67837/67841/67842/67873/67895):
  - `sn200k_ctrl_2r_v1` (g11): 30,330 GF/GPU — ANOMALOUS (RNG 143 s,
    solver 67 s; ~3-12x slow). Unresolved; full 2 s telemetry preserved.
  - `sn200k_ctrl_4r_v1` (g11): 128,478 GF/GPU (healthy).
  - `sn200k_ctrl_2r_v2` (g11, validation rerun per the documented
    replication clause): 206,923 GF/GPU (healthy; different PBS carve-out).
  - `sn200k_heavy_2r_v1` (g03, idle-holder 67044: 48cpu+4GPU+1TB):
    165,794 GF/GPU.
  - `sn200k_heavy_4r_v1` (g03): 130,440 GF/GPU (+1.5% vs control = noise).
  - `sn200k_light_2r_v1` (g12, co-tenant 67573: 24cpu+2GPU+500GB):
    209,246 GF/GPU (+1.1% vs control = noise; fastest 2r run).
  - `sn200k_light_4r_v1` — fielded on attempt a3 after user approval
    (option 1: g12 with a **moderate** 3-co-tenant composition, 48cpus +
    4GPUs + 1TB — heavier than light_2r's condition, caveat recorded).
    Job `67895` (11:15-11:17, 1:20): 157,631 GF/GPU — **+22.7% above the
    pristine control** (cross-node variance; every phase at-or-better than
    control, zero contention signature). Matrix now 6/6.
- Failed submissions (never started, qdelt with recorded reason, evidence
  preserved): 67824 (host=g11 must be hpc-gaas-g11 — Track-1 defect),
  67880 (g12 filled by new co-tenant 67869), 67882 (g10 went offline).
- All evidence retrieved to local `resource-alloc/outputs/sn200k_*`
  (46 files: .o/.e, pre/post captures, 2 s load samplers, presubmit
  snapshots).
- **Pinning verification runs** (user-approved, identical 2-rank setup,
  `COND=pinverify`, attempts `snver200k_2r_*`; unlogged during execution
  per user instruction, recorded here at wrap-up):
  - `snver200k_2r_g11_v1` (job 68008): never started — g11 filled with
    foreign jobs while queued; qdelt with recorded reason.
  - `snver200k_2r_g14_v1` (job 68009, g14 pristine, 5:59): granted the
    same socket-0 pair 4b/5c → NCCL resolved 48-49 → both ranks pinned to
    2 CPUs → **REPRODUCED** (RNG 75.3 s / LU 20.5 s / solver 42.6 s,
    42,251 GF/GPU vs the ~206k healthy band; cpus 48/49 at 100% for all
    75 in-app samples). Auto-NUMA thrash NOT required (25 pages/s vs v1's
    23.5k) — CPU starvation alone is the sufficient cause.
  - `snver200k_2r_g15_v1` (job 68017, g15 pristine): granted 9a/bb →
    resolved empty → healthy 205,778 GF/GPU. Confirms the trigger follows
    the granted GPU pair (4b/5c), not the node; the historical
    "g14/g15 never resolve" map was a grant-lottery artifact.

## Key findings

1. **Single-node co-tenant contention is absent**: heavy idle-holder +1.5%
   (4r), light co-tenant +1.1% (2r), and light/moderate 4r +22.7% *above*
   control (cross-node variance) vs controls — the multinode 3x4
   1.6-40x catastrophe does not reproduce on one node. The interference
   mechanism requires the inter-node dimension (staged comms path).
2. **The pathological pristine control (ctrl_2r_v1) is EXPLAINED** (user-
   directed verification): same node and pristine condition as the healthy
   v2, but a different granted GPU pair — v1's 4b/5c resolve in NCCL
   topology to CPU set 48-49, and the app pinned BOTH ranks to those
   2 CPUs (NCCL log narrowed-cpuset line; `Cpus_allowed_list=48-49` on all
   54 sampler samples; cpus 48/49 at 100% busy the whole app window).
   1 CPU/rank → RNG 3.6x / LU 2.9x / solver 11.9x slow, GPU starvation
   with full clocks, and auto-NUMA thrash (23.5k pages migrated/s vs 0.3
   in v2). Pinning to LARGE sets (heavy_4r/light_4r) is harmless — the
   failure mode is a set tiny relative to rank count. Blast radius checked:
   exp2's dirty250k v1/v2 carried one 2-CPU-pinned g11 rank (co-tenant
   conclusion still stands via exp5's pinning-free replication); clean v1.1-
   v3 and mech runs had ≥10-CPU single-rank pinning with no harm.
3. Healthy-node phase-time variance across nodes is large (2r RNG 29-53 s;
   4r headline 128.5-157.6k GF/GPU, 22.7% spread) at n=1 per cell; no
   occupied-node run fell below the healthy band.

## Decisions

- User (2026-09-18): N=200k/mem=500GB; single pass; proceed with live node
  checks avoiding the concurrent phase-2 workstream's nodes; full chain
  (commit/push/pull/qsub) authorized. `ctrl_2r_v2` validation rerun added
  autonomously under the documented "replicate if ambiguous" clause.
- Heavy-node re-pick g10→g03 (67564 ended mid-session); light-4r fielded on
  attempt a3 per user option 1 (g12, moderate composition, caveat recorded)
  after a1 (g12 co-tenant arrival) and a2 (g10 offline) failed.

## Blockers

- None. Experiment 6 matrix complete.

## Exact next action (resume)

Awaiting user direction: (1) test the pinning mitigation (wrapper's
explicit CPU-affinity input on a 4b/5c grant) and map whether other grant
shapes (1-GPU, socket-0 4-GPU sets) can resolve to small CPU sets;
(2) parent-track connection (post-GDR multinode pristine/busy pair);
(3) optional retrospective per-rank affinity audit of exp1-5 evidence.

## Evidence index

- All exp6 evidence: `scripts/gaas-internode-coms-debug/resource-alloc/
  outputs/sn200k_*` (byte-verified locally; 46 files).
- Pinning-verification evidence: `resource-alloc/outputs/snver200k_2r_*`
  (13 files; jobs 68009/68017 ran, 68008 never started).
- Record: `resource-alloc/SINGLE_NODE_TEST.md` (execution log, results,
  findings, limitations, suggested next).
- Commits: `dcb2c12` (plan+runner+pointers), `1af2085` (env hardening),
  plus this results commit.
