# Progress — 2026-09-09 session 4 (internode-coms Phase B2: collective diagnostic replication)

## Current step

Parent GPUDirect-RDMA debug track, Phase 1 Step 1 **Phase B2** (designed,
executed, and analyzed this session): replicated the full Phase B collective
GDR A/B ladder on clean host-pinned nodes with added coll/pml/UCC selection
diagnostics. All 4 jobs / 32 tests rc=0. Results and analysis are in
`scripts/gaas-internode-coms-debug/DEBUG_PROGRESS.md` (Phase B2 section);
the README resume point is updated. Both clones synced.

## What was completed

- **Plan agreed with user:** full Phase B matrix (all four rungs 2x1/2x2/3x1/
  3x4, one job per rung, sequential), Phase B tests unchanged; only deltas =
  diagnostic flags (`UCC_LOG_LEVEL=info`, `coll_base_verbose 100`,
  `pml_base_verbose 10`, `mpi_common_cuda_verbose 10`,
  `mpi_common_cuda_warning 1`, `ompi_info --all` pre-capture, per-rank
  GPU-UUID/affinity/env evidence) + pristine host-pinned nodes
  (`host=X:ngpus=4:ncpus=48:mem=1000GB` chunks, alloc-study shape).
  Observational scope only (no UCC disablement/threshold/transport changes).
  Ops policy carried from resource-alloc exp2/3 (qdel pre-auth for own stuck
  jobs, pristine-loss re-pick).
- **Scripts added:** `debug-scripts/phase1-step1/run_phase1_step1_collb2_
  {2x1,2x2,3x1,3x4}.pbs` + `collb2_node_snapshot.sh` (per-node checkpoint
  capture: loadavg, PSI, meminfo, cpuset, visible GPUs, in-job pbsnodes
  co-tenant listing); README Phase B2 design section.
- **Track 1 defect fixed mid-flight:** `step1_collb2_2x1_v1` (job `61084`)
  completed 8/8 but lost per-node ucxdev/nvtopo evidence (missing
  `export OUTDIR` before `-x OUTDIR`; trailing `true` masked it). Patched
  (+ missing-file guard, all 4 scripts) and rerun as v2. v1 evidence
  preserved and documented.
- **Node logistics:** waited for user's exp4 N-sweep (jobs 61055+) to release
  the trio; g14 then taken by another user's 12h full-node job (61071) →
  2-node rungs on pristine g16+g17; 3-node rungs on g16+g17+**g13
  (nearly pristine, user-approved: one light co-tenant, job 59192 = 12 CPUs +
  1 GPU, stable at all 8 checkpoints)**. g20 was a false pristine candidate
  (`gpu_ded` Qlist partition; also taken by 61097). In-job checkpoints
  recorded no other co-tenants; all H H negative controls clean.
- **Runs (all 8 tests rc=0 each):** `step1_collb2_2x1_v2` (job `61090`),
  `step1_collb2_2x2_v1` (`61091`), `step1_collb2_3x1_v1` (`61102`),
  `step1_collb2_3x4_v1` (`61104`). Evidence retrieved and byte-verified
  (81 files; 5 apparent mismatches were transient ssh failures, re-verified
  OK in a single batched call).
- **Key results:** Phase B's catastrophic ≥3-rank GDR collective pathology
  (13-44×) does **not** reproduce on clean nodes (6/8 cells healthy GDR, ctrl
  1.4-2.5× *faster* than staged) — Phase B tables were co-tenant
  contention-inflated. Two reproducible residuals remain: **3x4 bcast cuda
  ≥8 MiB 2.0-2.6× slower than staged** (single-rail zero-copy GDR selected)
  and **3x1 allreduce cuda @1 MiB 7.7×**. Diagnostic answer: `pml=ucx`;
  coll stack ucc(100)/hcoll(90)/cuda(78)/tuned(30); **UCC (TL_UCP) executes
  the collectives in both modes** (comm_select lines in `.e` + per-test UCC
  team create/destroy + score map `Cuda: {0..inf}:TL_UCP:10`).
- **Coordination incidents (recorded):** the concurrent resource-alloc
  session's `git add -A` swept 35 of my uncommitted collb2 evidence files
  into commit 753169f, then dropped them in d786585 (no data lost; files
  remained untracked locally); one GAAS `pull --ff-only` needed the
  documented byte-identical-duplicate removal before pulling. Both sessions
  share one local clone — commit evidence promptly.

## Evidence and blockers

- Evidence: `scripts/gaas-internode-coms-debug/outputs/phase1-step1/
  step1_collb2_*` (`.o`/`.e`, per-node ompiinfo/ucxdev/nvtopo, pre/prectrl/
  mid/post checkpoint logs, presched/postsched snapshots, and the v1-defect
  + blocked presched records).
- Git: commits `cd2462e` (scripts), `b87dc80` (v1 fix + v1 evidence),
  `7fdbae3` (all rung evidence); GAAS re-synced.
- No blockers.

## Exact next action

Await user direction on the recorded follow-ups (not executed):
(1) single-variable GDR-on + `--mca coll ^ucc` at 3x4/3x1 for both
residuals; (2) 3x4 bcast residual mechanism (single-rail zero-copy; rail/
memtype-cache/rndv-threshold knobs); (3) Track 2.2 re-scoped in-container
test looking for the 2-3× bcast-shaped residual on clean nodes.
