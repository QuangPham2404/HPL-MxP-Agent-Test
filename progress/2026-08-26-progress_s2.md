# Progress — 2026-08-26, session 2 (session close)

## Current step

Step 6/7 complete; session ending. The `omp-sweep` experiment is fully
extracted, analyzed, and synced. Awaiting user confirmation on the next
optimization direction.

## What was done this session

Session 1 (see `2026-08-26-progress.md`) resumed and completed the OMP sweep;
this closing session finalized the analysis artifacts:

1. Added a **pre-OMP reference comparison column** to both data tables in
   `planning/analysis/omp-sweep.md`, relative to the previous best
   `np-sweep` `2x4_row_491k` = 2.2067e+06 (N = 491520, NB = 3072, 2x4 row).
   The `vs baseline (1.4432e6)` column was retained alongside the new
   `vs prev best (2.2067e6)` column, and the section 2 scope / 3.x narrative /
   insights were updated to match. Committed as `48f078c`.

## Final state

- Both clones (local PC and GAAS) are synchronized at `48f078c` on `main`.
- `results/metrics.csv`: 121 rows (18 new `omp-sweep` rows; 103 → 121).
- Analysis: `planning/analysis/omp-sweep.md`; `planning/PLANS.md` updated with
  the omp-sweep direction row and next-direction narrative.
- `todo.md` on the local PC has unrelated pre-existing edits (left untouched,
  not committed).

## Key result for this workload

`OMP_NUM_THREADS=8` + `OMP_PLACES=sockets` + `OMP_PROC_BIND=TRUE` =
**2.3204e+06 GFLOP/s**:
- +60.79% over the original baseline (1.4432e+06).
- +5.15% over the pre-OMP reference (2.2067e+06).
- Highest single result recorded for this workload family.

## Next action (needs user confirmation)

1. Repeat the top `sockets` candidates (`TRUE`, `SPREAD`, `FALSE`) on comparable
   nodes to separate their ~1% ordering from node noise.
2. Then layer the matrix-placement lever (`--fill-device-buffer-size` /
   register step) onto the finalized OpenMP setting at N = 491520.

## Notes / blockers

- Node noise remains ~±1.5%; single-run per cell, so the ~1% spread among the
  top `sockets` candidates is not yet resolvable.
- GAAS still holds 6 untracked `N-sweep`/`N-resweep*_500000.{o,e}` user
  artifacts (left untouched).