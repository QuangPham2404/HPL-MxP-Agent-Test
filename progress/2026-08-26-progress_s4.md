# Progress — 2026-08-26, session 4 (matrix-placement-N-resweep)

## Current step

Step 2–6 complete for the `matrix-placement-N-resweep` experiment (N sweep under
`--fill-device 1`). All 5 runs retrieved, extracted (metrics.csv 139 → 144
rows), and analyzed in `planning/analysis/matrix-placement-N-resweep.md`.
Awaiting the user, who said they will revise the analysis plan and propose the
next step.

## What was done this session

1. Created `experiments/matrix-placement-N-resweep/` (README, `run_N_resweep.pbs`,
   `outputs/.gitkeep`); script fixes `--fill-device 1` + default buffer/register
   + OMP 8/sockets/TRUE and sweeps N. Committed `5a4acd1`, pushed, remote pulled.
2. Submitted N ∈ {491520 (control), 460800, 430080, 399360, 368640}: jobs
   53190–53194.
3. Retrieved 5 `.o`/`.e` pairs; ran `extract_sweeps.py` and `generate_results.py`.

## Key result

- Mechanism confirmed but net null: iterative-solver time 13.07 s → 4.09 s and
  its share 39.2% → 29.1% as N drops 491520 → 368640; GFLOP/s flat within node
  noise (LU efficiency loss cancels the solver gain).
- N=368640 (2.3792e+06, g19) is only +0.20% over N=491520 (2.3731e+06, g16) —
  parity, not a peak. Middle N (400k–460k) dips to ~2.28–2.34e+06.
- Hypothesis rejected as a net gain; N=491520 + `--fill-device 1` stands as the
  recommended config (~2.37–2.39e+06).

## Next action (awaiting user)

The user will revise the analysis plan and propose how to proceed. Candidate
orthogonal directions already listed in `planning/PLANS.md`: compute/precision
(`--preset-gemm-kernel`, `--sloppy-type`, `--Anq-device`), LU scheduling, and
panel-broadcast.

## Notes / blockers

- Node noise (±3–7%) across g16/g17/g18/g19 is the dominant error bar; the
  N=368640 vs N=491520 parity is not resolvable without repetition.
- GAAS still holds 6 pre-existing untracked `N-sweep`/`N-resweep*_500000.{o,e}`
  artifacts (left untouched).