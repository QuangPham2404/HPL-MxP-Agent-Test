# Progress — 2026-08-31 (MPI/NCCL communication sweep re-run)

## Current step

Re-running the full `experiments/mpi-nccl-coms-sweep` study (Phase 1 broadcast
policy sweep + Phase 2 U-panel chunk sweep) at the fixed best config, reusing
the existing scripts with `_reRun` appended to every output/error filename.
Phase 1 is complete; Phase 2 is still running. Per the user, results are to be
reported (raw GFLOP/s tables) only — **no `results/metrics.csv` write and no
analysis file at this stage.**

## What was done this session

1. Edited both scripts to append `_reRun` to all output/error files, exactly as
   requested:
   - Per-attempt files in `run_one()` now write
     `outputs/<label>_reRun.{o,e}` (previously `outputs/<label>.{o,e}`).
   - Added job-level `#PBS -o outputs/hpl_mxp_bc_sweep_reRun.{o,e}` (broadcast)
     and `#PBS -o outputs/hpl_mxp_chunk_sweep_reRun.{o,e}` (chunk), moving PBS
     job-level stdout/stderr into `outputs/`.
   - No other change: sweep sequences, fixed config, GPU-monitor flags, and the
     Phase-2 winners `--use-mpi-panel-broadcast` = `75`/`50` (already baked into
     `run_chunk_sweep.pbs`) are unchanged.
2. Syntax-checked, committed `3e97b6c`, pushed, and `git pull --ff-only` on GAAS.

## Phase 1 — broadcast policy sweep (COMPLETE)

- PBS job `55442`, node `hpc-gaas-g13`.
- All 7 attempts `PASSED`, residual `1.609604E-04` throughout (same as original).

| Attempt | broadcast | chunk | GFLOP/s | Evidence |
|---|---:|---:|---:|---|
| `bc_baseline_a` | 50 | 8 | 2.3214e+06 | `outputs/bc_baseline_a_reRun.{o,e}` |
| `bc_0` | 0 | 8 | 2.3177e+06 | `outputs/bc_0_reRun.{o,e}` |
| `bc_25` | 25 | 8 | 2.3603e+06 | `outputs/bc_25_reRun.{o,e}` |
| `bc_50` | 50 | 8 | 2.3695e+06 | `outputs/bc_50_reRun.{o,e}` |
| `bc_75` | 75 | 8 | 2.3740e+06 | `outputs/bc_75_reRun.{o,e}` |
| `bc_100` | 100 | 8 | 2.3856e+06 | `outputs/bc_100_reRun.{o,e}` |
| `bc_baseline_b` | 50 | 8 | 2.3571e+06 | `outputs/bc_baseline_b_reRun.{o,e}` |

Job-level evidence: `outputs/hpl_mxp_bc_sweep_reRun.{o,e}`.

## Phase 2 — U-panel chunk sweep (IN PROGRESS)

- PBS job `55450`, node `hpc-gaas-g13`, submitted after Phase 1 completed.
- Sweep: baseline (50/8) -> A=75 x {4,8,16} -> B=50 x {4,8,16} -> baseline (50/8).
- Progress as of handoff: **5 of 8 attempts complete** —
  `chunk_baseline_a`, `chunk4_a`, `chunk8_a`, `chunk16_a`, `chunk4_b` written.
- Remaining: `chunk8_b`, `chunk16_b`, `chunk_baseline_b`.

## Next action (for the continuing agent)

1. Poll `qstat -u pham0094` until job `55450` leaves the queue (bounded polling).
2. Confirm 8 `outputs/chunk*_reRun.o` files exist and each reports `PASSED` with
   a finite `GFLOPS = ...` and residual `1.609604E-04`.
3. Extract the Phase 2 GFLOP/s table (labels: `chunk_baseline_a`, `chunk4_a`,
   `chunk8_a`, `chunk16_a`, `chunk4_b`, `chunk8_b`, `chunk16_b`,
   `chunk_baseline_b`).
4. Report Phase 1 + Phase 2 results to the user. Do **not** write to
   `results/metrics.csv` or create an analysis file unless the user later asks.
5. Before finishing, update/append this progress note and commit+push+sync as
   the workflow requires.

## Git / PBS this session

- Commit `3e97b6c` (script edits). Remote synced to `3e97b6c`.
- PBS jobs: `55442` (Phase 1, complete), `55450` (Phase 2, running).