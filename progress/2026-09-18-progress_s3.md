# Progress — 2026-09-18 s7 (Phase 2 Stage 2: container GDR A/B — COMPLETE)

## Current step

Phase 2 Stage 2 (container-native OSU/NCCL GDR A/B, 2x1 → 3x4) **complete**:
all four scored cells ran and both container stacks show working GPUDirect
RDMA. Results, verdicts, and evidence recorded in
`scripts/gaas-internode-coms-debug/DEBUG_PROGRESS_P2.md` → "Phase 2 — Stage
2". Stage 3 (HPL-MxP integration A/B at N=480000/NB=1024 vs
`3x4-baseline_v1`) awaits user go.

## Completed this session

- Matrix confirmed with user: 4 jobs (one per stack × rung, both GDR arms
  same job), cleanest-eligible node hunting (`gpu_as`/`gpu_ded`), group
  `hpc_ebslee`, one job at a time.
- Scripts created: `debug-scripts/phase2-stage2/` (parameterized
  `run_phase2_stage2_{osu,nccl}_common.sh` + 4 topology wrappers +
  `run_phase2_stage2_nccl_3x4_hca.pbs` contingency wrapper +
  `fabric_capture.sh` + README with attempt log).
- Scored runs (all PASS, full evidence retrieved to
  `scripts/gaas-internode-coms-debug/outputs/phase2-stage2/`, 149 files,
  107 MB, committed and synced):
  - `phase2_stage2_osu_2x1_v2` (`67956.gaas`, g14+g15 pristine `gpu_as`):
    8/8; D D bw @4 MiB ctrl 50,969 vs gdroff 38,585 MB/s (+32.1%); D D
    latency @8 B 9.34 vs 19.39 µs (2.08×); H H controls clean (−0.03%);
    proto: ctrl `rendezvous zero-copy read from remote` rc_mlx5 74/26 vs
    gdroff `rendezvous cuda_copy, frag host` — GDR confirmed.
  - `phase2_stage2_nccl_2x1_v3` (`67965.gaas`, g20+g22 `gpu_ded`): 6/6,
    all 3 tests AB_VALID (ctrl 16 IB / 8 GDRDMA vs gdroff 16/0); @64 MiB
    sendrecv +15.0%, broadcast +8.2%, allreduce +21.2%.
  - `phase2_stage2_osu_3x4_v1` (`67969.gaas`, g22+g20+g03, pt2pt pair
    g22+g20): 8/8; same zero-copy vs staging switch; NEW anomaly recorded:
    on the g22+g20 pair GDR-on D D latency ≥1 MiB is 9.5–16.7× slower than
    staged and D D bw @4 MiB −7% (Case-A-like, pair-dependent; deferred,
    not a GDR failure).
  - `phase2_stage2_nccl_3x4_v2` (`67974.gaas`, same trio, `NCCL_IB_HCA`
    bond-excluded both arms): 6/6, all 3 tests AB_VALID (sendrecv 16/24,
    broadcast 48/72, allreduce 448/616 GDRDMA channels; gdroff 0);
    @64 MiB sendrecv +42.3%, broadcast +149% (2.49×), allreduce algbw
    +142% (2.42×; busbw 81.03 vs 33.45 GB/s).
- Container ≈ host everywhere (within ±5% on every NCCL cell; OSU D D at
  57.7% of H H ceiling vs host 57.3%).
- Bond-abort contingency (case `2026-09-17-A` analog) exercised on
  `nccl_3x4_v1` (`67973.gaas`): ctrl allreduce rc=3 with the exact
  signature; fixed via the pre-authorized host-validated HCA bond
  exclusion. Upstream-report-worthy (plugin aborts instead of excluding the
  incompatible rail), now reproduced in-container.
- Track 1 fixes (evidence preserved, all recorded in the README attempt
  log): `SHARED_STAGE` hyphen typo (`67922`); health-gate OSU path +
  `libverifiable` over-strict gate (`67958`); node misjudgment —
  `pbsnodes -aSj` f/t columns are FREE/total, g13 was fully occupied
  (`67963`, never started, qdelt with reason).
- Remote sync issue resolved safely: the pull aborted on 149 untracked
  remote evidence copies; all verified byte-identical to the committed
  blobs (git hash-object), directory renamed to
  `outputs/.phase2-stage2.quarantine_identical_20260918` (preserved;
  awaiting user-approved deletion), pull completed, tracked tree diff-verified.

## Key findings

1. **Container GPUDirect RDMA is CONFIRMED on both stacks** (MPI/UCX
   zero-copy rc_mlx5 + NCCL per-channel GDRDMA with valid A/B controls) —
   the 2026-09-03 gdrdrv clue is superseded for the RDMA path. Combined
   with Phase 1: host and container infrastructure both deliver GDR.
2. The container NCCL stack behaves like the host's (same IBext_v11 plugin,
   same GDR-channel fractions, same bond-abort failure mode and fix).
3. Open items (recorded, non-blocking): in-container GDR-on rendezvous
   latency anomaly on the g22+g20 pair (Case-A-like); pair-dependent D D
   p2p bandwidth (g14+g15 51 GB/s vs ~37.8 on other pairs); deferred
   host UCX items (Case A rendezvous-scheme/chunk; socket-floor control).

## Decisions

- User: 4-job packaging (stack × rung); cleanest-node hunting with
  contention recording; full execution chain authorized (commit/push/pull,
  qsub, monitoring, retrieval, analysis, records).
- Bond-abort contingency applied exactly as pre-authorized (signature
  matched; full-test-set rerun with HCA bond-excluded both arms).

## Blockers

- None.

## Exact next action (resume)

Awaiting user go for Phase 2 Stage 3: HPL-MxP integration A/B — small
N=120000/NB=1024 launch-validation case first (with default communication
settings + path diagnostics), then the N=480000/NB=1024 same-allocation A/B
(defaults vs GDR disabled in both UCX and NCCL using the Stage 2-validated
knobs `UCX_IB_GPU_DIRECT_RDMA=n` + `NCCL_NET_GDR_LEVEL=LOC`), preserving
`3x4-baseline_v1` as the comparison reference (report paired A/B change
and % vs that baseline). Per the locked Stage 1 decisions: `--skip-tests 1`
+ monitoring flags, ~2:30 walltime for the validation case, scripts under
`debug-scripts/phase2-stage3/`, outputs under `outputs/phase2-stage3/`.
Housekeeping option for user: delete the verified-identical quarantine dir
`outputs/.phase2-stage2.quarantine_identical_20260918` (remote) once
reviewed.

## Evidence index

- Raw evidence: `scripts/gaas-internode-coms-debug/outputs/phase2-stage2/`
  (149 files; per-arm/per-test logs, .o/.e per attempt, rank maps, ABI,
  fabric, checkpoints, hostfiles, presched).
- Records: `DEBUG_PROGRESS_P2.md` → "Phase 2 — Stage 2" (tables, verdicts,
  evidence index); `debug-scripts/phase2-stage2/README.md` (attempt log).
- Jobs: 67922 (T1 fail), 67956 (PASS), 67958 (T1 fail), 67963 (never
  started), 67965 (PASS), 67969 (PASS), 67973 (contingency), 67974 (PASS).
- Commits: `e8e06da`, `6fd2aee`, `c4c227b`, `528b918`, `fd9fbd3` (+ this
  progress commit).
