# Progress — 2026-09-26: Workflow v2 methodology reconciliation

## Scope and authorization

The user directly authorized documentation/control-plane reconciliation and
GitHub push. No optimization task was selected or created. This request
authorized the bounded documentation changes without authorizing experiment
design decisions, strategic analysis of run evidence, GAAS access, PBS jobs,
or a new optimization direction.

## Completed work

- Added the active project-specific GAAS/HPL-MxP adapter at
  `workflow/08-Workflow-Multinode-Tuning.md`, retaining the existing launch,
  evidence, provenance, directory, and error-handling gates.
- Reconciled ownership and handoffs across root guidance, the workflow README,
  blueprint, dependency-graph README, and user sweep guide: Strategic Analyst
  designs and interprets; the Human Leader approves; Codex orchestrates only
  synchronized approved tasks; OpenCode workers perform substantive execution.
- Placed dependency checkpoints after the execution-report handoff and explicit
  `ANALYSE_RESULTS`. Review or explicit skip is recorded, followed by exactly
  one recommendation and a human decision. Graph edges grant no execution
  authority; further experiments require approved bounded task scope.
- Applied the user's future scored-run consensus: `--skip-tests 0` and
  `--monitor-gpu 0`. Separately authorized monitoring remains diagnostic.
- Made `workflow_old/` entirely historical; preserved the prior Setup review
  and appended its resolution. Added a historical-context notice to the prior
  single-node optimization plan without changing its scientific body.
- Preserved `3x4-baseline_v1` as the original baseline and documented its
  historical protocol mismatch. Comparisons disclose that mismatch and retain
  a separately measured same-protocol in-sweep control.

Two OpenCode workers handled non-overlapping documentation assignments, with
bounded follow-ups for explicit lifecycle, skip, and materialization wording.
Codex inspected the complete changes and performed the final validation.

## Validation and preservation

- `git diff --check` passed; 73 local Markdown link targets resolved.
- Blueprint phase overview through Phase 5, all blueprint headings, checkpoint
  full/light/closed definitions, and user-guide scientific tables are unchanged.
- Dependency-graph scientific sections and `edges.csv` are byte-identical.
- General workflow `00`–`07`, the task template, old adapter, historical
  experiments/scripts/outputs/results/analyses, and earlier progress records
  are unchanged. The Setup review was preserved by an append-only update.
- Required repository-wide search terms were checked. Matches were classified
  as active and correct, historical evidence (including superseded-policy
  disclosures and launch-validation models), or intentionally diagnostic.
  No unresolved integration conflict was found. The old-adapter path remains
  only in the preserved historical Setup review.
- Pre-existing untracked `hpl-mxp-runs-on-gaas/` remains untouched and excluded
  from the commit.

## Handoff

Documentation reconciliation is complete. No optimization action is selected.
Future execution requires an explicitly identified, human-approved,
synchronized task and the project-specific command permissions. The historical
baseline protocol caveat remains an explicit comparison limitation, not a
reason to replace evidence or promote a baseline.
